---
title: "MJ1: Multimodal Judgment via Grounded Verification"
title_zh: MJ1：通过落地验证实现多模态评判
authors: "Bhavesh Kumar, Dylan Feng, Leonard Tang"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.07990v1"
tags: ["query:mmfnd"]
score: 8.0
evidence: 用于接地验证和主张评估的多模态裁判模型
tldr: "MJ1是一个通过强化学习训练的多模态评判模型，旨在解决现有模型在视觉证据关联上的不足。它引入了结构化的“观察-主张-验证-评估-评分”验证链，并结合反事实一致性奖励来消除位置偏差。实验表明，仅有3B参数的MJ1在MMRB2基准测试上达到了77.0%的准确率，超越了Gemini-3-Pro等超大规模模型，证明了视觉关联和一致性训练在提升评判能力方面的有效性。"
selection_source: fresh_fetch
motivation: 现有的多模态评判模型在将决策与视觉证据进行关联（视觉接地）方面存在困难。
method: 提出了一种结构化的接地验证链，并利用强化学习结合反事实一致性奖励来优化模型并减少位置偏差。
result: "3B参数的MJ1在MMRB2上准确率达到77.0%，在图像编辑和多模态推理任务上显著提升，并超越了更大规模的模型。"
conclusion: 通过接地验证和一致性训练，可以在不增加模型规模的情况下显著提升多模态评判的准确性和可靠性。
---

## 摘要
多模态评判模型难以将决策建立在视觉证据之上。我们提出了 MJ1，这是一个通过强化学习训练的多模态评判模型，它通过结构化的落地验证链（观察 $\rightarrow$ 主张 $\rightarrow$ 验证 $\rightarrow$ 评估 $\rightarrow$ 评分）强制执行视觉落地，并采用惩罚位置偏差的反事实一致性奖励。即使不经过训练，我们的机制也能将基础模型在 MMRB2 上的准确率在图像编辑任务中提高 3.8 个百分点，在多模态推理任务中提高 1.7 个百分点。经过训练后，仅拥有 3B 激活参数的 MJ1 在 MMRB2 上达到了 77.0% 的准确率，超越了像 Gemini-3-Pro 这样规模大出几个数量级的模型。这些结果表明，落地验证和基于一致性的训练可以在不增加模型规模的情况下显著提升多模态评判能力。

## Abstract
Multimodal judges struggle to ground decisions in visual evidence. We present MJ1, a multimodal judge trained with reinforcement learning that enforces visual grounding through a structured grounded verification chain (observations $\rightarrow$ claims $\rightarrow$ verification $\rightarrow$ evaluation $\rightarrow$ scoring) and a counterfactual consistency reward that penalizes position bias. Even without training, our mechanism improves base-model accuracy on MMRB2 by +3.8 points on Image Editing and +1.7 on Multimodal Reasoning. After training, MJ1, with only 3B active parameters, achieves 77.0% accuracy on MMRB2 and surpasses orders-of-magnitude larger models like Gemini-3-Pro. These results show that grounded verification and consistency-based training substantially improve multimodal judgment without increasing model scale.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **MJ1**，一个专门为多模态评判（Multimodal Judgment）设计的强化学习训练模型。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管多模态大模型（VLM）发展迅速，但作为“评判者”（Judge）来评估图像生成或编辑质量时，其表现远逊于纯文本评判者。
*   **核心瓶颈**：
    *   **视觉接地（Visual Grounding）失效**：模型在深度推理时，对视觉 Token 的注意力会剧烈衰减，导致评分往往基于文本偏见而非图像内容。
    *   **位置偏差（Position Bias）**：模型倾向于给排在前面的选项（Response A）打高分。
    *   **幻觉问题**：模型在长文本推理中容易脱离视觉事实。
*   **研究目标**：开发一种无需极大参数规模，就能通过结构化推理和强化学习实现高精度、视觉关联性强的多模态评判模型。

### 2. 方法论
MJ1 的核心由**落地验证链**和**强化学习训练**两部分组成：

*   **落地验证链（Grounded Verification Chain）**：
    强制模型按顺序执行以下步骤：
    1.  **视觉观察 (O)**：在注意力最集中的初始阶段描述图像内容。
    2.  **主张提取 (C)**：从待评价的回复中提取可验证的陈述。
    3.  **一致性验证 (V)**：将主张与视觉观察逐一比对，产生二进制（0/1）信号。
    4.  **准则评估 (E)**：基于验证结果，对照特定任务（如图像编辑、推理）的准则进行评价。
    5.  **最终评分 (s)**：给出 1-10 的整数分（不允许平局）。

*   **训练流水线**：
    1.  **冷启动 SFT**：使用 1 万条蒸馏出的推理轨迹进行监督微调，确立格式和基础能力。
    2.  **GRPO 强化学习**：使用群体相对策略优化，奖励函数包含三部分：
        *   **格式奖励 ($R_{format}$)**：验证 XML 标签的完整性。
        *   **准确性奖励 ($R_{correct}$)**：是否与人类/金标准偏好一致。
        *   **反事实一致性奖励 ($R_{cons}$)**：这是本文创新点。将输入图像和回复顺序调换，要求模型给出的偏好结论也必须相应反转。如果两次结果不一致，则惩罚模型。

### 3. 实验设计
*   **基准测试**：主要使用 **MMRB2** (Multimodal RewardBench 2)，包含四个子任务：文本转图像 (T2I)、图像编辑、交错生成、多模态推理。
*   **对比对象**：
    *   **开源模型**：Gemma 3 (4B-27B), Qwen2.5-VL (7B-72B), Qwen3-VL (8B-235B)。
    *   **闭源 API 模型**：GPT-4o, GPT-5, Gemini 2.5/3 Pro。
*   **消融/验证实验**：
    *   对比了“开放式推理”与“落地验证链”在未训练模型上的表现。
    *   **图像条件实验**：测试模型在“真实图像”、“随机打乱图像”和“空白图像”下的表现，以验证模型是否真的在看图。

### 4. 资源与算力
*   **基础模型**：Qwen3-VL-30B-A3B（混合专家模型 MoE，总参数 30B，每 Token 激活参数仅 **3B**）。
*   **训练细节**：使用 LoRA（rank 64）进行微调。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量或总训练时长，但提到 GRPO 训练时每组生成 32 个样本，这通常需要相当规模的计算资源（如 A100/H100 集群）。

### 5. 实验数量与充分性
*   **数据规模**：训练集包含 7 万个样本（来自 Rapidata, EditReward, RLAIF-V 等）。
*   **实验覆盖**：在 MMRB2 的 4000 个测试样本上进行了全面评估。
*   **充分性评价**：实验设计较为严谨。特别是通过“打乱图像”导致一致性奖励大幅下降的实验，有力地证明了该方法确实增强了视觉关联，而非仅仅是文本层面的逻辑自洽。对比了从 3B 到 235B 的多代模型，具有很强的说服力。

### 6. 主要结论与发现
*   **以小博大**：仅有 3B 激活参数的 MJ1 在 MMRB2 上达到 **77.0%** 的准确率，超越了参数量大得多的 Gemini-3-Pro 和 GPT-5。
*   **结构胜过规模**：结构化的落地验证链即使不经过训练，也能提升基础模型约 1.7-3.8% 的准确率。
*   **消除偏差**：反事实一致性奖励成功将模型的位置偏差降低到接近零（Response A 和 B 的选择概率趋于均等）。
*   **视觉注意力**：通过在生成初期强制进行视觉观察，有效缓解了 VLM 常见的视觉信息丢失问题。

### 7. 优点
*   **高效性**：证明了通过精巧的推理链设计和 RL 训练，小模型可以超越超大规模模型。
*   **可解释性**：结构化的输出（观察->主张->验证）让评判过程透明，用户可以清晰看到模型为何给出特定分数。
*   **鲁棒性**：引入反事实一致性奖励，解决了 LLM 评判中长期存在的位置偏见顽疾。

### 8. 不足与局限
*   **“一致性演戏”（Consistency Theater）风险**：虽然实验有所反驳，但理论上模型仍可能学会生成内部逻辑自洽但与图像事实无关的“假推理”来骗取奖励。
*   **任务依赖性**：该方法的有效性高度依赖于训练数据中是否存在必须通过视觉证据才能解决的“硬样本”。
*   **推理开销**：由于引入了复杂的五阶段推理链，MJ1 的推理 Token 数远多于直接评分模型，导致推理延迟和成本增加。

（完）

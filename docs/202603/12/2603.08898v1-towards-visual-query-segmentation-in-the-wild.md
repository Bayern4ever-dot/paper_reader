---
title: Towards Visual Query Segmentation in the Wild
title_zh: 迈向野外环境下的视觉查询分割
authors: "Bing Fan, Minghao Li, Hanzhi Zhang, Shaohua Dong, Naga Prudhvi Mareedu, Weishi Shi, Yunhe Feng, Yan Huang, Heng Fan"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08898v1"
tags: ["query:tsds"]
score: 8.0
evidence: 未修剪视频中的视觉查询分割
tldr: 本文提出了视觉查询分割（VQS）这一新范式，旨在根据外部视觉查询在长视频中实现全时段、像素级的目标定位。为推动该领域研究，作者构建了包含4111个视频和130万帧的大规模基准数据集VQS-4K。同时，提出了一种名为VQ-SAM的有效方法，通过自适应记忆生成模块扩展了SAM 2，在处理目标特征和背景干扰方面表现优异，显著提升了分割性能。
selection_source: fresh_fetch
motivation: 现有的视觉查询定位仅限于边界框且通常只关注最后一次出现，无法满足现实场景中对全面且精确的像素级定位需求。
method: 提出了VQ-SAM框架，通过多阶段架构和自适应记忆生成（AMG）模块扩展SAM 2，利用视频中的目标特定线索和背景干扰信息。
result: 在VQS-4K基准测试中，VQ-SAM取得了显著优于现有方法的性能，证明了其在复杂视频环境下的有效性。
conclusion: VQS-4K和VQ-SAM的推出打破了传统定位范式的局限，为未来在野外环境下的视觉查询分割研究和实际应用奠定了基础。
---

## 摘要
在本文中，我们引入了视觉查询分割（Visual Query Segmentation, VQS），这是视觉查询定位（Visual Query Localization, VQL）的一种新范式，旨在给定外部视觉查询的情况下，对未剪辑视频中感兴趣对象的所有像素级出现进行分割。与现有的仅使用边界框定位目标最后一次出现的 VQL 相比，VQS 能够实现更全面（即所有对象出现）和更精确（即像素级掩码）的定位，使其在现实场景中更具实用性。为了推动该任务的研究，我们提出了 VQS-4K，这是一个专门用于 VQS 的大规模基准测试集。具体而言，VQS-4K 包含 4,111 个视频，拥有超过 130 万帧，涵盖了 222 个不同的对象类别。每个视频都配有一个由搜索视频之外的帧及其目标掩码定义的视觉查询，并标注了与查询目标相对应的时空掩码序列（masklets）。为确保高质量，VQS-4K 中的所有视频均经过细致检查和迭代优化的人工标注。据我们所知，VQS-4K 是首个专门为 VQS 设计的基准测试集。此外，为了激发未来的研究，我们提出了一种简单而有效的方法，名为 VQ-SAM。该方法通过利用视频中特定于目标的线索和背景干扰线索，通过一个带有自适应记忆生成（AMG）模块的新型多阶段框架来逐步演化记忆，从而扩展了 SAM 2，显著提升了 VQS 的性能。在 VQS-4K 上的广泛实验中，VQ-SAM 取得了令人鼓舞的结果，并超越了所有现有方法，证明了其有效性。借助提出的 VQS-4K 和 VQ-SAM，我们期望超越当前的 VQL 范式，并启发更多关于 VQS 的未来研究和实际应用。我们的基准测试集、代码和结果将公开发布。

## Abstract
In this paper, we introduce visual query segmentation (VQS), a new paradigm of visual query localization (VQL) that aims to segment all pixel-level occurrences of an object of interest in an untrimmed video, given an external visual query. Compared to existing VQL locating only the last appearance of a target using bounding boxes, VQS enables more comprehensive (i.e., all object occurrences) and precise (i.e., pixel-level masks) localization, making it more practical for real-world scenarios. To foster research on this task, we present VQS-4K, a large-scale benchmark dedicated to VQS. Specifically, VQS-4K contains 4,111 videos with more than 1.3 million frames and covers a diverse set of 222 object categories. Each video is paired with a visual query defined by a frame outside the search video and its target mask, and annotated with spatial-temporal masklets corresponding to the queried target. To ensure high quality, all videos in VQS-4K are manually labeled with meticulous inspection and iterative refinement. To the best of our knowledge, VQS-4K is the first benchmark specifically designed for VQS. Furthermore, to stimulate future research, we present a simple yet effective method, named VQ-SAM, which extends SAM 2 by leveraging target-specific and background distractor cues from the video to progressively evolve the memory through a novel multi-stage framework with an adaptive memory generation (AMG) module for VQS, significantly improving the performance. In our extensive experiments on VQS-4K, VQ-SAM achieves promising results and surpasses all existing approaches, demonstrating its effectiveness. With the proposed VQS-4K and VQ-SAM, we expect to go beyond the current VQL paradigm and inspire more future research and practical applications on VQS. Our benchmark, code, and results will be made publicly available.

---

## 论文详细总结（自动生成）

这是一份关于学术论文《Towards Visual Query Segmentation in the Wild》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：传统的视觉查询定位（VQL）任务主要关注在长视频中寻找目标的“最后一次出现”，且通常仅提供粗糙的边界框（Bounding Box）。
*   **核心痛点**：在现实场景（如视频编辑、精准监控）中，仅定位最后一次出现是不够的，且边界框包含过多背景噪声，无法提供精确的像素级目标边界。
*   **核心问题**：论文提出了**视觉查询分割（Visual Query Segmentation, VQS）**这一新范式。其目标是：给定一个视频外部的查询图像及掩码，在未剪辑视频中识别并分割出该目标**所有**出现的时刻，并提供**像素级**的掩码。
*   **任务挑战**：相比视频对象分割（VOS），VQS 的查询图来自视频外部，缺乏连续性；相比 VQL，它要求更高的空间精度和更全面的时间覆盖。

### 2. 论文提出的方法论：VQ-SAM
*   **核心思想**：基于 SAM 2 架构进行扩展，通过**多阶段（Multi-stage）**框架逐步演化记忆库，同时挖掘视频中的“目标特定线索”和“背景干扰项线索”。
*   **关键技术细节**：
    *   **多阶段演化**：模型分为 $K$ 个阶段（实验中 $K=2$）。第一阶段利用初始查询生成初步预测，后续阶段利用前一阶段的结果提取更丰富的特征。
    *   **目标特征生成（TFG）**：从视频帧中筛选高置信度的目标预测掩码，提取特征以应对目标在视频中的外观变化。
    *   **干扰项特征生成（DFG）**：识别视频中与目标相似但并非目标的区域（Distractors），提取特征以增强模型区分相似物体的能力。
    *   **自适应记忆生成（AMG）**：这是核心模块。它利用多层感知机（MLP）动态学习初始查询、目标特征和干扰项特征的权重（$w_1, w_2, w_3$），加权融合生成下一阶段的增强记忆。
    *   **时空 Transformer（STT）**：在掩码解码前引入 STT 块，增强视频帧之间的全局时空上下文关联。

### 3. 实验设计
*   **数据集（Benchmark）**：
    *   **VQS-4K**：作者构建的大规模基准，包含 4,111 个视频（130万帧），涵盖 222 个目标类别。
    *   **标注质量**：采用人工多轮迭代精细标注，确保像素级精度。
    *   **数据集划分**：分为训练集（3,289个视频）和测试集（822个视频），并按目标大小分为小、中、大三个子集。
*   **对比方法**：
    *   **VOS 方法**：Cutie, OASIS, SAM 2, SAM 2.1++, SAM 3, SAM2Long 等。
    *   **VQL 方法**：VQLoC, PRVQL, REN（这些方法被修改以输出掩码）。
*   **评价指标**：stAP（时空平均精度）、tAP（时间平均精度）、Recovery（恢复率）和 Success（成功率）。

### 4. 资源与算力
*   **硬件环境**：使用 4 张 **Nvidia A6000 (48GB)** GPU。
*   **训练时长**：在 VQS-4K 训练集上训练约 **62 小时**。
*   **推理效率**：推理速度为 **29 FPS**，处理 8 帧视频片段时显存占用约为 9.7GB。

### 5. 实验数量与充分性
*   **实验规模**：论文在 822 个测试视频上进行了全面的性能评估，并对比了 10 种主流基准模型。
*   **消融实验**：作者进行了极其详尽的消融实验（约 10 组表），涵盖了 TFG/DFG 模块的作用、STT 块的影响、阶段数 $K$ 的选择、AMG 权重策略、以及超参数 $n_t, n_d$ 的敏感度分析。
*   **客观性与公平性**：为了公平对比，所有可训练的对比模型均在 VQS-4K 上重新训练。此外，作者还在传统的 VQ2D 任务上验证了 VQ-SAM 的泛化能力，证明其在边界框定位任务上同样领先。

### 6. 主要结论与发现
*   **性能领先**：VQ-SAM 在 VQS-4K 上取得了 26.0% 的 stAP，比最强的对比模型（SAM2Long）高出 7.4%。
*   **干扰项的重要性**：实验证明，显式建模背景干扰项（DFG）对减少误报、提升分割精度至关重要。
*   **记忆演化的有效性**：多阶段演化显著优于单阶段预测，说明通过视频内容反馈来修正记忆库是处理“野外环境”长视频的关键。
*   **任务难度**：即使是 SOTA 模型，在 VQS 任务上的 stAP 也仅为 26% 左右，说明该任务极具挑战性。

### 7. 优点
*   **范式创新**：首次明确定义了视觉查询分割（VQS）任务，比传统的 VQL 更符合实际应用需求。
*   **基准贡献**：提供了目前首个专门针对 VQS 的大规模、高质量、多类别（222类）数据集 VQS-4K。
*   **架构设计**：AMG 模块的设计非常巧妙，能够根据视频内容自适应地平衡“初始记忆”与“在线学习特征”的关系。

### 8. 不足与局限
*   **长视频处理限制**：受限于显存，模型仍需将长视频切分为短片段处理，这可能导致超长距离的时间一致性减弱。
*   **性能空间**：在小物体（Small-scale）上的表现（stAP 18.8%）远低于大物体（36.0%），说明对微小物体的捕捉仍是瓶颈。
*   **计算开销**：多阶段框架虽然提升了精度，但也成倍增加了推理时的计算负担，对于实时性要求极高的场景可能存在限制。

（完）

---
title: "FreqCycle: A Multi-Scale Time-Frequency Analysis Method for Time Series Forecasting"
title_zh: FreqCycle：一种用于时间序列预测的多尺度时频分析方法
authors: "Boya Zhang, Shuaijie Yin, Huiwen Zhu, Xing He"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09661v1"
tags: ["query:tsds"]
score: 8.0
evidence: 时间序列的分段频域模式学习
tldr: 本研究针对时间序列预测中中高频特征被忽视的问题，提出了FreqCycle框架。该框架包含FECF模块以提取低频周期模式，以及SFPL模块通过可学习滤波器增强中高频能量。为应对复杂的耦合多周期性，进一步扩展为MFreqCycle，通过跨尺度交互解耦嵌套特征。实验证明，该方法在七个基准数据集上达到了SOTA精度，并兼顾了推理效率。
selection_source: fresh_fetch
motivation: 现有深度学习模型多关注低频模式而忽视中高频特征，且难以处理时间序列中复杂的耦合多周期性。
method: 提出FreqCycle框架，结合周期预测增强模块（FECF）和分段频域模式学习模块（SFPL），并扩展为多尺度架构。
result: 在七个领域的基准测试中，该方法在保持较快推理速度的同时，实现了最先进的预测精度。
conclusion: FreqCycle通过多尺度时频分析有效提升了预测性能，在准确性和效率之间取得了理想平衡。
---

## 摘要
挖掘时频特征对于时间序列预测至关重要。现有研究主要集中在对低频模式进行建模，而时间序列的大部分能量都集中于此。对中高频特征的忽视持续限制了深度学习模型的进一步性能提升。我们提出了 FreqCycle，这是一个集成以下模块的新颖框架：(i) 滤波器增强型周期预测 (FECF) 模块，通过在时域中显式学习共享周期模式来提取低频特征；(ii) 分段频域模式学习 (SFPL) 模块，通过可学习滤波器和自适应加权来增强中高频能量比例。此外，时间序列数据通常表现出耦合的多周期性，例如交织的周周期和日周期。为了应对耦合的多周期性以及长回看窗口的挑战，我们将 FreqCycle 分层扩展为 MFreqCycle，通过跨尺度交互来解耦嵌套的周期性特征。在七个不同领域的基准测试上进行的广泛实验表明，FreqCycle 在保持更快推理速度的同时实现了最先进的准确率，在性能和效率之间达到了最佳平衡。

## Abstract
Mining time-frequency features is critical for time series forecasting. Existing research has predominantly focused on modeling low-frequency patterns, where most time series energy is concentrated. The overlooking of mid to high frequency continues to limit further performance gains in deep learning models. We propose FreqCycle, a novel framework integrating: (i) a Filter-Enhanced Cycle Forecasting (FECF) module to extract low-frequency features by explicitly learning shared periodic patterns in the time domain, and (ii) a Segmented Frequency-domain Pattern Learning (SFPL) module to enhance mid to high frequency energy proportion via learnable filters and adaptive weighting. Furthermore, time series data often exhibit coupled multi-periodicity, such as intertwined weekly and daily cycles. To address coupled multi-periodicity as well as long lookback window challenges, we extend FreqCycle hierarchically into MFreqCycle, which decouples nested periodic features through cross-scale interactions. Extensive experiments on seven diverse domain benchmarks demonstrate that FreqCycle achieves state-of-the-art accuracy while maintaining faster inference speeds, striking an optimal balance between performance and efficiency.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **FreqCycle** 的新型时间序列预测框架，旨在通过深入挖掘中高频特征和处理多尺度耦合周期性来提升预测精度。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **研究背景**：时间序列预测（TSF）在能源、气象和经济等领域至关重要。
*   **核心痛点**：
    1.  **忽视中高频特征**：现有模型（尤其是基于 MLP 的模型）大多关注能量集中的低频模式，而忽略了包含短期波动和非周期性特征的中高频成分。
    2.  **显式周期学习不足**：许多模型试图通过复杂架构学习长程依赖，而非直接建模显而易见的周期性（如日、周周期）。
    3.  **耦合多周期性挑战**：现实数据中常存在嵌套周期（如日周期嵌套在周周期内），现有架构难以在长回看窗口下有效解耦并建模这些特征。

### 2. 方法论：核心思想与关键技术
论文提出了 **FreqCycle** 及其扩展版 **MFreqCycle**，核心组件包括：
*   **FECF（滤波器增强型周期预测）**：
    *   在时域中显式学习一个全局共享的周期基准（Periodic Basis）。
    *   通过自适应滤波器（FFT -> 学习权重 -> iFFT）增强低频周期模式，同时抑制可能干扰周期学习的高频噪声。
*   **SFPL（分段频域模式学习）**：
    *   受短时傅里叶变换（STFT）启发，将残差信号分段并应用滑动窗口。
    *   在频域对各段进行可学习的滤波和自适应加权求和，旨在**放大中高频成分的能量比例**，从而捕捉非周期性的细微波动。
*   **MFreqCycle（多尺度扩展）**：
    *   采用分层并行架构，分别处理不同尺度的周期（如基础周期模块处理日周期，周周期模块处理宏观模式）。
    *   利用池化层降低长序列计算开销，并通过预测融合层自适应整合多尺度结果。

### 3. 实验设计
*   **数据集**：涵盖 7 个主流基准数据集：ETT 系列（ETTh1, ETTh2, ETTm1, ETTm2）、Electricity (ECL)、Weather、Traffic。
*   **对比方法（Baselines）**：
    *   **线性/MLP 类**：DLinear, CycleNet, TimeMixer, SparseTSF。
    *   **频域学习类**：FreTS, FilterNet, FITS, Amplifier。
    *   **Transformer 类**：iTransformer, PatchTST。
*   **评估指标**：平均绝对误差（MAE）和均方误差（MSE）。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA RTX 4090 GPU (24GB)**。
*   **软件环境**：PyTorch 2.6.0。
*   **训练细节**：训练 30 个 epoch，使用了 Adam 优化器，搜索了不同的学习率（0.005-0.03）和隐藏层维度（128, 512）。
*   **复杂度**：理论复杂度为 $O(L \log L)$，在推理速度和内存占用上表现优异。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个数据集上进行了多长度预测（96, 192, 336, 720）的全面测试。
*   **消融实验**：分别验证了 FECF 和 SFPL 模块的有效性，并对比了不同季节性分解方法（如 MOV, LD）。
*   **参数敏感性**：研究了不同窗口类型（矩形、汉宁、汉明窗）和窗口长度对 SFPL 性能的影响。
*   **可视化分析**：提供了频谱增强前后的对比图以及模型学习到的周期模式可视化。
*   **评价**：实验设计全面，对比了从经典到 2025 年最新的多种模型，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：FreqCycle 在 14 项指标中获得 10 项第一，显著优于现有的 SOTA 模型。
*   **中高频的重要性**：通过 SFPL 增强中高频能量，模型在处理非平稳和具有丰富瞬态特征的数据（如 ETTh）时表现更佳。
*   **长窗口优势**：MFreqCycle 在长回看窗口（如 672 步）下，通过解耦嵌套周期，比简单增加窗口长度的其他模型有更显著的性能提升。
*   **效率平衡**：在保持高精度的同时，其推理速度和内存效率远优于 Transformer 类模型。

### 7. 优点与亮点
*   **理论结合实际**：将信号处理中的 STFT 理论巧妙引入深度学习框架，解决了频域能量分布不均的问题。
*   **显式建模**：FECF 模块通过参数化建模直接捕捉周期性，避免了黑盒模型学习长程依赖的困难。
*   **多尺度解耦**：MFreqCycle 为处理复杂的时间序列嵌套结构提供了一种高效的并行方案。

### 8. 不足与局限
*   **超参数依赖**：基础周期 $W$ 和 SFPL 的窗口长度需要根据数据集特性手动或通过搜索确定，自动化程度有待提高。
*   **长周期验证受限**：由于现有基准数据集的时间跨度限制，模型对年周期等更长尺度模式的建模能力尚未得到充分验证。
*   **应用场景**：主要针对具有明显周期性或准周期性的工业/自然科学数据，对于完全随机或极度稀疏的时间序列效果可能受限。

（完）

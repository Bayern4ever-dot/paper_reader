---
title: "mAVE: A Watermark for Joint Audio-Visual Generation Models"
title_zh: mAVE：一种用于联合音视频生成模型的水印
authors: "Luyang Si, Leyi Pan, Lijie Wen"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07090v1"
tags: ["query:mmfnd"]
score: 9.0
evidence: 联合音视频生成与深伪绑定
tldr: "本文提出mAVE水印框架，旨在解决联合音视频生成模型中因模态解耦导致的“绑定漏洞”。mAVE通过逆变换采样在初始化阶段将音视频潜变量加密绑定，无需微调即可定义合法纠缠流形。实验表明，该方法在不损失生成质量的前提下，能实现超99%的绑定完整性，有效防御音视频替换攻击。"
selection_source: fresh_fetch
motivation: 现有水印技术因模态独立验证，无法防御将伪造音频替换入水印视频的“替换攻击”。
method: 提出mAVE框架，利用逆变换采样在初始化阶段将音视频潜变量在流形上进行加密纠缠绑定。
result: "在主流模型上实现了超99%的绑定完整性，且保证了生成性能无损。"
conclusion: mAVE为联合生成模型提供了一种鲁棒的加密防御方案，有效保障了厂商版权和内容溯源。
---

## 摘要
随着联合音视频生成模型的大规模商业部署，嵌入水印已成为保护厂商版权和确保内容溯源的关键。然而，现有技术由于将不同模态视为解耦实体，存在架构不匹配的问题，从而暴露了严重的绑定漏洞（Binding Vulnerability）。攻击者通过替换攻击（Swap Attacks）利用这一漏洞，在保留带水印视频的同时，将真实音频替换为恶意的深度伪造内容。由于目前的检测器依赖于独立验证（$Video_{wm}\vee Audio_{wm}$），它们会错误地认证被篡改的内容，将有害媒体错误地归因于原始厂商，从而严重损害其声誉。为了解决这一问题，我们提出了 mAVE（流形音视频纠缠），这是首个专为联合架构设计的原生水印框架。mAVE 在初始化阶段通过逆变换采样定义合法纠缠流形，在无需微调的情况下对音频和视频的潜变量进行加密绑定。在最先进模型（LTX-2, MOVA）上的实验表明，mAVE 保证了性能无损，并针对替换攻击提供了指数级的安全界限。mAVE 实现了近乎完美的绑定完整性（>99%），为厂商版权提供了稳健的加密防御。

## Abstract
As Joint Audio-Visual Generation Models see widespread commercial deployment, embedding watermarks has become essential for protecting vendor copyright and ensuring content provenance. However, existing techniques suffer from an architectural mismatch by treating modalities as decoupled entities, exposing a critical Binding Vulnerability. Adversaries exploit this via Swap Attacks by replacing authentic audio with malicious deepfakes while retaining the watermarked video. Because current detectors rely on independent verification ($Video_{wm}\vee Audio_{wm}$), they incorrectly authenticate the manipulated content, falsely attributing harmful media to the original vendor and severely damaging their reputation. To address this, we propose mAVE (Manifold Audio-Visual Entanglement), the first watermarking framework natively designed for joint architectures. mAVE cryptographically binds audio and video latents at initialization without fine-tuning, defining a Legitimate Entanglement Manifold via Inverse Transform Sampling. Experiments on state-of-the-art models (LTX-2, MOVA) demonstrate that mAVE guarantees performance-losslessness and provides an exponential security bound against Swap Attacks. Achieving near-perfect binding integrity ($>99\%$), mAVE offers a robust cryptographic defense for vendor copyright.

---

## 论文详细总结（自动生成）

以下是对论文《mAVE: A Watermark for Joint Audio-Visual Generation Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的音视频生成模型水印技术存在“模态解耦”的问题。虽然视频和音频是联合生成的，但水印通常是独立嵌入的。
*   **研究动机**：这种解耦导致了**绑定漏洞（Binding Vulnerability）**。攻击者可以实施**替换攻击（Swap Attack）**，即保留带水印的视频，但将音频替换为恶意的深度伪造（Deepfake）内容。由于现有检测器独立验证各模态（$Video_{wm} \vee Audio_{wm}$），它们会错误地认证被篡改的内容，导致厂商声誉受损。
*   **整体含义**：论文提出了 **mAVE（流形音视频纠缠）**，旨在通过加密手段在生成过程的初始化阶段将音视频模态强制绑定，确保两者必须来自同一次生成会话。

### 2. 论文提出的方法论
*   **核心思想**：利用基于 ODE（常微分方程）采样器的数学可逆性，在初始噪声空间构建一个“合法纠缠流形”。
*   **关键技术细节**：
    *   **会话感知密钥派生**：利用服务器端的私钥和会话索引，通过 HMAC 和 SHA-256 生成会话密钥 $K_{sess}$。
    *   **纠缠位网格构造**：构造视频网格 $B_v$ 和音频网格 $B_a$。关键在于音频网格中嵌入了视频网格的加密哈希摘要（$h_v = \text{SHA-256}(B_v)$），从而在位级别建立依赖。
    *   **逆变换采样（Inverse Transform Sampling）**：将加密后的二进制水印位映射到连续的 Gaussian 潜变量空间。通过将位 0 映射到负半高斯分布，位 1 映射到正半高斯分布，确保初始噪声在统计上仍符合标准正态分布。
    *   **联合反演检测**：检测时，通过联合 ODE 反演将生成的音视频内容推回初始噪声空间，提取位网格并验证哈希绑定关系。

### 3. 实验设计
*   **使用模型**：最先进的开源联合音视频生成模型 **LTX-2** 和 **MOVA-720p**。
*   **应用场景**：文本生成音视频（T2AV）和图像生成音视频（I2AV）。
*   **数据集/Benchmark**：从 **VBench** 中精选了 250 个文本提示词，涵盖动物、建筑、食物、人类、生活方式、植物、风景、车辆 8 个语义类别。
*   **对比方法**：
    *   **视频水印**：VideoShield, VideoSeal。
    *   **音频水印**：AudioSeal, WavMark, Timbre。
    *   **安全基准**：弱基准（独立验证）和强基准（独立验证 + SyncNet 语义同步检查）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **效率分析**：由于 mAVE 是**无需训练（Training-free）**的方法，它不需要昂贵的模型微调过程。检测阶段利用了 Rectified Flow 的直线轨迹特性，实验表明仅需 5 步 ODE 反演即可实现高精度检测，计算开销与单模态视频水印相当。

### 5. 实验数量与充分性
*   **实验规模**：共生成了 1,000 个多样化的音视频样本（250 个提示词 $\times$ 4 个随机种子）。
*   **消融实验**：对哈希长度 $N$（16 到 256 位）和反演步数（1 到 50 步）进行了详细测试。
*   **鲁棒性测试**：涵盖了视频攻击（帧平均、噪声、模糊、缩放、亮度调节）和音频攻击（MP3 压缩、低通滤波、重采样、量化）。
*   **充分性评价**：实验设计较为充分，不仅验证了水印的提取精度，还通过 VBench 指标证明了生成质量的“无损性”，并针对替换攻击提供了严谨的概率界限分析，实验结果具有客观性。

### 6. 论文的主要结论与发现
*   **性能无损**：理论证明并实验确认，mAVE 嵌入的水印在统计上与标准高斯采样不可区分，不会降低视频的视觉质量或音视频同步性。
*   **极高的安全性**：mAVE 提供了针对替换攻击的指数级安全界限。在默认配置下，误报率（FP）低于 $10^{-10}$。
*   **绑定完整性**：实现了超过 99% 的绑定准确率，远超基于语义同步（如 SyncNet）的启发式防御方法。
*   **反演效率**：在 Rectified Flow 架构下，极少数的反演步数（5步）即可保持极高的位准确率（>95%）。

### 7. 优点
*   **架构原生设计**：首个针对联合音视频生成架构设计的水印方案，解决了模态解耦的根本安全隐患。
*   **无需训练**：作为一种“处理中（In-processing）”水印，它不需要对生成模型进行昂贵的重训练或微调。
*   **数学严谨性**：提供了关于性能无损（Theorem 1）和安全界限（Theorem 2）的严格数学证明。
*   **高效性**：检测过程简单，且能同时提取双模态水印，无需额外的音频编码器。

### 8. 不足与局限
*   **离散化漂移**：由于 ODE 数值求解的截断误差，原始位准确率无法达到理论上的 100%（通常饱和在 0.9 左右），虽然这不影响统计检测，但在极端情况下可能存在波动。
*   **时间维度鲁棒性限制**：由于采用了固定的重复因子，该方法对剧烈的时间维度攻击（如大幅度改变帧率或插帧）较为敏感，容易导致时间索引对齐失效。
*   **应用限制**：目前主要针对基于 Rectified Flow 或 ODE 采样的扩散模型，对于非连续潜变量空间的生成模型（如基于离散 Token 的模型）可能不直接适用。

（完）

---
title: Evidence Packing for Cross-Domain Image Deepfake Detection with LVLMs
title_zh: 基于 LVLM 的跨域图像 Deepfake 检测证据打包
authors: "Yuxin Liu, Fei Wang, Kun Li, Yiqi Nie, Junjie Chen, Zhangling Duan, Zhaohong Jia"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17761v1"
tags: ["query:mmfnd"]
score: 9.0
evidence: 利用大视觉语言模型进行跨域图像深度伪造检测
tldr: 针对图像伪造检测（IDD）中大模型微调成本高且泛化性差的问题，本文提出了一种无需训练的框架SCEP。该方法通过挖掘具有代表性的可疑图像块（Evidence Pack），结合全局语义不一致性与局部频率/噪声异常进行评分与采样，引导冻结的视觉语言大模型（LVLM）进行推理。实验证明，该方法在多个基准测试上优于现有基线，显著提升了跨域检测性能。
selection_source: fresh_fetch
motivation: 现有的视觉语言大模型在图像伪造检测中面临微调成本高昂且难以应对多样化、不断演变的篡改手段的挑战。
method: 提出SCEP框架，通过聚类图像块并结合语义不一致性与频率噪声指标筛选关键证据块，引导冻结的LVLM进行推理。
result: 在多个跨域基准测试中，该方法在无需任何微调的情况下，性能优于多种强基线模型。
conclusion: 证明了通过精心筛选的局部证据块可以有效激发冻结LVLM的伪造检测能力，为跨域深度伪造检测提供了高效的新思路。
---

## 摘要
图像 Deepfake 检测 (IDD) 通过识别合成或篡改伪影，将操纵图像与真实图像区分开来。尽管大语言视觉模型 (LVLMs) 具有强大的图像理解能力，但将其应用于 IDD 通常需要昂贵的微调，且对多样化、不断演变的操纵手段泛化能力较差。我们提出了语义一致性证据包 (SCEP)，这是一种无需训练的 LVLM 框架，它用基于证据的推理取代了全图推理。SCEP 挖掘出一组紧凑的可疑补丁标记 (patch tokens)，这些标记能最好地揭示操纵线索。它利用视觉编码器的 CLS 标记作为全局参考，将补丁特征聚类为连贯的组，并使用融合指标对补丁进行评分，该指标结合了 CLS 引导的语义不匹配以及基于频率和噪声的异常。为了覆盖分散的痕迹并避免冗余，SCEP 在每个聚类中采样少量高置信度补丁，并应用基于网格的非极大值抑制 (NMS)，生成一个证据包，作为冻结 LVLM 进行预测的条件。在多个基准测试上的实验表明，SCEP 在无需微调 LVLM 的情况下优于强基准模型。

## Abstract
Image Deepfake Detection (IDD) separates manipulated images from authentic ones by spotting artifacts of synthesis or tampering. Although large vision-language models (LVLMs) offer strong image understanding, adapting them to IDD often demands costly fine-tuning and generalizes poorly to diverse, evolving manipulations. We propose the Semantic Consistent Evidence Pack (SCEP), a training-free LVLM framework that replaces whole-image inference with evidence-driven reasoning. SCEP mines a compact set of suspicious patch tokens that best reveal manipulation cues. It uses the vision encoder's CLS token as a global reference, clusters patch features into coherent groups, and scores patches with a fused metric combining CLS-guided semantic mismatch with frequency-and noise-based anomalies. To cover dispersed traces and avoid redundancy, SCEP samples a few high-confidence patches per cluster and applies grid-based NMS, producing an evidence pack that conditions a frozen LVLM for prediction. Experiments on diverse benchmarks show SCEP outperforms strong baselines without LVLM fine-tuning.

---

## 论文详细总结（自动生成）

以下是对论文《Evidence Packing for Cross-Domain Image Deepfake Detection with LVLMs》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在不进行昂贵微调的情况下，利用大视觉语言模型（LVLM）实现高效、鲁棒的跨域图像深度伪造检测（IDD）。
*   **研究动机**：
    *   **微调成本高**：现有的 LVLM 适配 IDD 任务通常需要大量高质量标注数据进行微调，计算开销大。
    *   **泛化性差**：微调后的模型往往难以应对不断演变的伪造技术。
    *   **线索丢失**：通用的标记（Token）选择策略倾向于保留语义信息，容易忽略微小的伪造痕迹（如边缘不连续、频率异常等）。
    *   **推理冗余**：全图推理包含大量无关背景，干扰模型对伪造区域的判断。

### 2. 方法论：语义一致性证据包 (SCEP)
SCEP 是一种**无需训练（Training-free）**的框架，其核心思想是将全图推理转变为“证据驱动”的推理。
*   **语义聚类**：利用冻结视觉编码器的 CLS Token 作为全局锚点，通过球面 K-means 算法将图像补丁（Patch）聚类为 $K$ 个语义连贯组，确保后续对比在相似语义背景下进行。
*   **多维异常评分**：为每个补丁计算综合可疑得分 $S_{r,c}$，融合了三个维度的指标：
    1.  **语义差异（Semantic Discrepancy）**：计算补丁与全局 CLS Token 的余弦距离，识别语义不一致区域。
    2.  **频率异常（Frequency Anomaly）**：通过 2D DCT 变换获取频谱，利用 Jensen-Shannon 散度（JSD）衡量局部频谱与全局平均频谱的偏差。
    3.  **噪声异常（Noise Anomaly）**：通过高斯平滑提取残差图，利用中值（Median）和绝对中位差（MAD）量化高频残差的不一致性。
*   **证据挖掘与去冗余**：在每个聚类中筛选得分最高的补丁，并应用基于网格的非极大值抑制（NMS），剔除空间位置重叠的冗余补丁，形成最终的“证据包”。
*   **LVLM 推理**：将精简后的证据包（Token 序列）与特定提示词（Prompt）输入冻结的 LVLM，由模型给出“真”或“假”的最终裁定。

### 3. 实验设计
*   **数据集**：使用大规模基准测试 **DFBench**，涵盖三个子集：
    *   **AI-generated**：纯 AI 生成图像（如 SD3.5, Flux, PixArt 等）。
    *   **AI-edited**：局部篡改图像（涉及对象增强、风格改变、语义修改等）。
    *   **Real Source**：真实图像源。
*   **对比基线**：
    *   **原始 LVLM 推理**：包括 Kimi-VL, Gemma-3, MiniCPM-V, Janus-Pro, Owl2.1, Qwen3-VL 等。
    *   **Token 剪枝方法**：对比了 A-ViT 和 DynamicViT。
*   **评价指标**：准确率（Acc%）和 F1 分数。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA A6000 GPU** 和 Intel Xeon Gold 6330 CPU 的服务器上运行。
*   **训练时长**：由于该方法是 **Training-free（无需训练）** 的，因此没有训练耗时。
*   **推理效率**：论文报告了推理延迟，SCEP 相比原始模型推理速度提升了约 **1.58 倍**（例如 Qwen3-VL 从 21.44s 降至 13.56s）。

### 5. 实验数量与充分性
*   **实验规模**：在 10 余个主流 AI 生成模型和多种篡改场景下进行了测试，涵盖了从 7B 到 16B 参数规模的 6 种不同 LVLM 后端。
*   **消融实验**：针对超参数 $\alpha$（平衡低级线索与语义差异的权重）进行了多组对比实验。
*   **充分性评价**：实验设计较为全面，不仅验证了检测性能的提升，还通过可视化案例展示了证据抓取的准确性，并评估了推理效率，客观地证明了该方法在跨域场景下的有效性。

### 6. 主要结论与发现
*   **性能提升**：SCEP 在无需微调的情况下，显著提升了所有测试 LVLM 的检测能力。在 AI 生成子集上，Gemma-3 的 Acc 提升至 54.44%；在 AI 篡改子集上，Qwen3-VL 表现最优。
*   **证据有效性**：实验证明，局部伪造痕迹（频率和噪声异常）比全局语义信息更能有效暴露 Deepfake 风险。
*   **效率优势**：通过大幅减少输入 Token 的数量，SCEP 在提升精度的同时降低了计算延迟。

### 7. 优点（亮点）
*   **即插即用**：无需任何参数更新，可直接应用于各种预训练的视觉语言大模型。
*   **多模态线索融合**：巧妙地结合了计算机视觉传统特征（频率、噪声）与深度学习语义特征，弥补了 LVLM 对底层统计特性不敏感的缺陷。
*   **抗冗余性**：通过聚类和 NMS 确保了证据的多样性和代表性，避免了模型被重复的背景信息误导。

### 8. 不足与局限
*   **依赖基础模型**：检测效果仍受限于所选 LVLM 本身的推理和理解能力。
*   **超参数敏感性**：权重 $\alpha$ 的选择可能需要根据不同的伪造类型进行微调，以达到最佳平衡。
*   **极端场景挑战**：对于极其微小且经过后处理（如多次压缩）的伪造痕迹，基于补丁的采样可能会存在漏检风险。

（完）

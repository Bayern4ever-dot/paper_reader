---
title: "TMTE: Effective Multimodal Graph Learning with Task-aware Modality and Topology Co-evolution"
title_zh: TMTE：基于任务感知的模态与拓扑协同演化的有效多模态图学习
authors: "Yinlin Zhu, Xunkai Li, Di Wu, Wang Luo, Miao Hu, Di Wu"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27723v1"
tags: ["query:mmfnd"]
score: 8.0
evidence: 多模态图学习与表示
tldr: 针对多模态属性图（MAG）中存在的拓扑噪声、连接缺失及任务无关性问题，本文提出了一种任务感知的模态与拓扑协同演化框架（TMTE）。该框架利用模态与拓扑之间的双向耦合关系，通过多视角度量学习优化图结构，并结合平滑正则化融合优化模态表示，在多种图中心和模态中心任务上均达到了SOTA性能。
selection_source: fresh_fetch
motivation: 现实世界中的多模态图存在拓扑质量缺陷且缺乏任务针对性，导致通用关系图难以适配多样化的下游任务。
method: 提出TMTE框架，通过多视角度量学习演化拓扑，并结合跨模态对齐与平滑正则化演化模态表示，实现两者的闭环协同优化。
result: 在9个多模态图数据集和1个非图数据集上的6类任务中，TMTE均一致地取得了当前最先进的性能表现。
conclusion: 实验证明，通过任务驱动的模态与拓扑联合迭代优化，能有效解决多模态图学习中的结构质量问题并提升表征效果。
---

## 摘要
多模态属性图（MAGs）是多模态图学习（MGL）的基础数据结构，支持以图为中心和以模态为中心的任务。然而，我们的实证分析揭示了现实世界 MAGs 中固有的拓扑质量限制，包括噪声交互、缺失连接以及与任务无关的关系结构。因此，由通用关系导出的单一图结构不太可能对各种下游任务都达到全局最优。为应对这一挑战，我们提出了任务感知的模态与拓扑协同演化（TMTE），这是一种新型的 MGL 框架，旨在针对目标任务联合且迭代地优化图拓扑和多模态表示。TMTE 的动机源于模态与拓扑之间的双向耦合：多模态属性诱导关系结构，而图拓扑塑造模态表示。具体而言，TMTE 将拓扑演化建模为基于锚点近似的模态嵌入多视角度量学习，并将模态演化公式化为带有跨模态对齐的平滑正则化融合，从而产生闭环的任务感知协同演化过程。在 9 个 MAG 数据集和 1 个非图多模态数据集上进行的 6 个以图为中心和以模态为中心任务的广泛实验表明，TMTE 一致地实现了最先进的性能。我们的代码可在 https://anonymous.4open.science/r/TMTE-1873 获取。

## Abstract
Multimodal-attributed graphs (MAGs) are a fundamental data structure for multimodal graph learning (MGL), enabling both graph-centric and modality-centric tasks. However, our empirical analysis reveals inherent topology quality limitations in real-world MAGs, including noisy interactions, missing connections, and task-agnostic relational structures. A single graph derived from generic relationships is therefore unlikely to be universally optimal for diverse downstream tasks. To address this challenge, we propose Task-aware Modality and Topology co-Evolution (TMTE), a novel MGL framework that jointly and iteratively optimizes graph topology and multimodal representations toward the target task. TMTE is motivated by the bidirectional coupling between modality and topology: multimodal attributes induce relational structures, while graph topology shapes modality representations. Concretely, TMTE casts topology evolution as multi-perspective metric learning over modality embeddings with an anchor-based approximation, and formulates modality evolution as smoothness-regularized fusion with cross-modal alignment, yielding a closed-loop task-aware co-evolution process. Extensive experiments on 9 MAG datasets and 1 non-graph multimodal dataset across 6 graph-centric and modality-centric tasks show that TMTE consistently achieves state-of-the-art performance. Our code is available at https://anonymous.4open.science/r/TMTE-1873.

---

## 论文详细总结（自动生成）

### TMTE：基于任务感知的模态与拓扑协同演化的多模态图学习深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
多模态属性图（MAGs）在金融、生物化学和推荐系统等领域应用广泛。然而，论文指出当前多模态图学习（MGL）面临三大核心挑战：
*   **拓扑质量限制**：现实中的图结构往往存在**噪声交互**（虚假连接）、**缺失连接**（信息不全）以及**任务无关性**（通用关系未必适配特定下游任务）。
*   **完美拓扑假设的失效**：现有方法大多假设输入图是完美的，但实验证明，分类任务需要语义一致性，而生成任务可能需要语义多样性，单一拓扑无法通用。
*   **模态与拓扑的解耦**：现有研究往往忽略了模态属性与图拓扑之间的双向耦合关系。

#### 2. 方法论：核心思想与技术细节
论文提出了 **TMTE (Task-aware Modality and Topology co-Evolution)** 框架，通过迭代优化实现模态表示与图拓扑的闭环协同演化：
*   **拓扑演化（Topology Evolution）**：
    *   **多视角度量学习**：利用加权余弦相似度从多个维度捕捉节点间的潜在关系。
    *   **锚点近似（Anchor-based Approximation）**：为了解决大规模图的计算开销（$O(N^2)$），引入随机采样的锚点集，将复杂度降低至 $O(N \cdot |U|)$。
    *   **残差修正**：演化后的拓扑作为原始拓扑的残差补充，而非完全替代。
*   **模态演化（Modality Evolution）**：
    *   **平滑正则化融合**：基于图信号处理（GSP）理论，通过最小化拉普拉斯正则项，使融合后的节点表示在演化拓扑上保持平滑。
    *   **幂级数近似**：利用 Neumann 级数展开（公式 7）获得闭式解的截断近似，避免了复杂的矩阵求逆。
    *   **跨模态对齐**：通过对比学习损失（$L_{mod}$）确保各模态嵌入与融合表示之间的一致性。
*   **任务感知协同优化**：将下游任务损失（$L_{task}$）与模态对齐损失联合优化，使拓扑和表示共同向目标任务收敛。

#### 3. 实验设计
*   **数据集**：涵盖 9 个 MAG 数据集（如 RedditS, Movies, Grocery, Toys, SemArt, Flickr30k 等）和 1 个非图数据集（MVSA），涉及社交网络、电商、艺术、音视频等多个领域。
*   **任务场景**：
    *   **图中心任务**：节点分类、链接预测、节点聚类。
    *   **模态中心任务**：模态检索、图转文本生成（G2Text）、图转图像生成（G2Image）。
*   **对比方法（Baselines）**：
    *   **单模态 GNN**：GCN, GCNII, GAT, GATv2。
    *   **多模态图学习方法**：MMGCN, LGMRec, GraphGPT-O, Graph4MM, InstructG2I, UniGraph2 等 12 种前沿方法。

#### 4. 资源与算力
*   **硬件环境**：工作站配备 Intel Xeon 可扩展处理器，**NVIDIA RTX 6000 Ada Generation GPU (96GB VRAM)**，以及 256GB RAM。
*   **软件环境**：CUDA 12.9, Python 3.10.18, PyTorch 2.8。
*   **效率表现**：在 Movies 数据集上，TMTE 的单轮训练时间约为 0.4225s，推理时间 0.1099s。尽管计算开销略高于轻量级模型（如 NTSFormer），但其参数量极小（仅 0.5M），具有较好的部署性。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 10 个数据集上进行了详尽测试，涵盖了从基础分类到复杂生成任务的 6 大类别。
*   **消融实验**：对比了“单次拓扑演化”、“仅模态演化”和“任务无关演化”三种变体，证实了协同演化和任务感知机制的必要性。
*   **鲁棒性与超参分析**：针对拓扑噪声（随机加边/删边）进行了压力测试，并对平滑率 $\alpha$、截断步数 $T$、视角数 $K$ 等关键参数进行了敏感性分析。
*   **客观性**：实验采用了统一的特征编码器（Qwen2-VL-7B）和评估协议，每组实验重复三次取均值，确保了对比的公平性。

#### 6. 主要结论与发现
*   **协同演化的优越性**：TMTE 在所有 6 类任务中均一致地超越了现有 SOTA 方法。在节点分类上 Accuracy 最高提升 3.83%，在 G2Image 任务上 CLIP-S 提升显著。
*   **拓扑自适应性**：实验发现，分类任务受益于“类别一致性”拓扑，而生成任务受益于“模态对齐”拓扑。TMTE 能够根据任务自动学习最合适的结构。
*   **非图数据的适用性**：即使在没有预定义图结构的 MVSA 数据集上，TMTE 也能通过演化机制构建出有效的关联结构，表现优于传统 MGL 方法。

#### 7. 优点（亮点）
*   **理论支撑扎实**：结合了图信号处理（GSP）的平滑性理论与度量学习，并给出了收敛性和稳定性的数学证明（定理 B.1-B.4）。
*   **解决核心痛点**：打破了“输入图即真理”的局限，通过闭环迭代修复了现实数据中的拓扑缺陷。
*   **扩展性强**：锚点机制和递归幂级数计算使得模型能够处理大规模图数据，且参数量极低。

#### 8. 不足与局限
*   **集成探索不足**：论文尚未探讨将 TMTE 机制集成到其他更复杂的 MGL 骨干网络（如超图神经网络）中的效果。
*   **计算延迟**：虽然参数量小，但由于存在多轮迭代演化，其单轮训练时长相比于固定拓扑的方法仍有数倍的增加。
*   **超大规模验证**：虽然理论上支持大规模图，但在超大规模分布式环境下的实际表现（如数亿级节点）仍有待进一步验证。

（完）

---
title: "Bridging Pixels and Words: Mask-Aware Local Semantic Fusion for Multimodal Media Verification"
title_zh: 连接像素与文字：用于多模态媒体验证的掩码感知局部语义融合
authors: "Zizhao Chen, Ping Wei, Ziyang Ren, Huan Li, Xiangru Yin"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26052v1"
tags: ["query:mmfnd"]
score: 10.0
evidence: 多模态误导信息检测与验证
tldr: 针对多模态虚假信息检测中全局对齐容易忽略局部语义冲突的问题，本文提出MaLSF框架。该框架利用掩码-标签对作为语义锚点，通过双向跨模态验证（BCV）模块显式定位冲突，并利用层次化语义聚合（HSA）模块进行推理。实验证明该方法在DGM4和虚假新闻检测任务上达到了SOTA水平，具有良好的解释性。
selection_source: fresh_fetch
motivation: 现有的全局融合方法因“特征稀释”效应，容易掩盖细微的局部语义不一致，难以识别复杂的虚假信息。
method: 提出MaLSF框架，利用双向跨模态验证模块（BCV）和层次化语义聚合模块（HSA），通过掩码-标签对实现像素与单词的主动比对。
result: 在DGM4和多模态虚假新闻检测任务上均取得了最先进的性能，并通过消融实验和可视化验证了其有效性。
conclusion: MaLSF通过模拟人类认知的交叉验证机制，有效解决了多模态验证中局部冲突检测的难题。
---

## 摘要
随着多模态虚假信息变得日益复杂，对其进行检测和定位至关重要。然而，当前依赖被动整体融合的多模态验证方法在处理复杂的虚假信息时面临挑战。由于“特征稀释”，全局对齐往往会平滑掉细微的局部语义不一致，从而有效地掩盖了其原本旨在发现的冲突。我们提出了 MaLSF（掩码感知局部语义融合），这是一个将范式转变为主动双向验证的新型框架，模拟了人类认知的交叉引用过程。MaLSF 利用掩码-标签对作为语义锚点来连接像素和文字。其核心机制包含两项创新：1）双向跨模态验证（BCV）模块，充当询问者，利用并行查询流（文本作为查询和图像作为查询）来明确指出冲突；2）分层语义聚合（HSA）模块，智能地聚合这些多粒度冲突信号以进行特定任务的推理。此外，为了提取细粒度的掩码-标签对，我们引入了一组多样化的掩码-标签对提取解析器。MaLSF 在 DGM4 和多模态虚假新闻检测任务上均达到了最先进的性能。广泛的消融实验和可视化结果进一步验证了其有效性和可解释性。

## Abstract
As multimodal misinformation becomes more sophisticated, its detection and grounding are crucial. However, current multimodal verification methods, relying on passive holistic fusion, struggle with sophisticated misinformation. Due to 'feature dilution,' global alignments tend to average out subtle local semantic inconsistencies, effectively masking the very conflicts they are designed to find. We introduce MaLSF (Mask-aware Local Semantic Fusion), a novel framework that shifts the paradigm to active, bidirectional verification, mimicking human cognitive cross-referencing. MaLSF utilizes mask-label pairs as semantic anchors to bridge pixels and words. Its core mechanism features two innovations: 1) a Bidirectional Cross-modal Verification (BCV) module that acts as an interrogator, using parallel query streams (Text-as-Query and Image-as-Query) to explicitly pinpoint conflicts; and 2) a Hierarchical Semantic Aggregation (HSA) module that intelligently aggregates these multi-granularity conflict signals for task-specific reasoning. In addition, to extract fine-grained mask-label pairs, we introduce a set of diverse mask-label pair extraction parsers. MaLSF achieves state-of-the-art performance on both the DGM4 and multimodal fake news detection tasks. Extensive ablation studies and visualization results further verify its effectiveness and interpretability.

---

## 论文详细总结（自动生成）

### 论文总结：连接像素与文字——用于多模态媒体验证的掩码感知局部语义融合 (MaLSF)

#### 1. 核心问题与整体含义
论文针对多模态虚假信息（如 Deepfake 图像配虚假文字）检测中的**“特征稀释”（Feature Dilution）**问题展开研究。现有的方法通常采用“被动整体融合”范式，将图像和文本编码为全局向量后融合。这种方式会导致局部的细微语义冲突（例如：运动员拿着香槟庆祝的图像配上“比赛失败”的文字）被全局对齐的背景信息淹没。

**研究动机**：模拟人类的“主动验证”认知过程，通过在局部区域（像素）与特定词汇（文字）之间建立显式的双向比对，精准捕捉细微的跨模态不一致性。

#### 2. 方法论
MaLSF 框架将验证过程从被动融合转变为主动询问，核心流程如下：
*   **语义锚点提取（Mask-Label Pairs）**：引入两种解析器（Open Vocabulary Parser 和 Caption-Anchored Parser），利用大模型（如 OMG-LLaVA 或 GLIP+SAM2）提取图像中的局部掩码及其对应的文本标签，作为跨模态比对的基准。
*   **双向跨模态验证 (BCV) 模块**：
    *   **以图查文 (Image-as-Query)**：利用全局图像特征查询文本标签，检测图像内容与文字描述的冲突。
    *   **以文查图 (Text-as-Query)**：利用原始标题查询图像局部区域，寻找视觉证据是否支持文字主张。
    *   **门控机制**：通过计算相关性权重，自动过滤无关的局部语义噪声。
*   **层次化语义聚合 (HSA) 模块**：
    *   **浅层融合**：将 [CLS] 标记（全局共识）与序列标记（细粒度细节）解耦处理，防止信息稀释。
    *   **深层融合**：针对不同任务（二分类、多标签分类、图像/文本定位）生成特定的解耦特征。

#### 3. 实验设计
*   **任务与数据集**：
    *   **DGM4 任务**：检测并定位多模态操纵。使用 DGM4 数据集（230k 图像-文本对），包含人脸交换、属性篡改等。
    *   **MFND 任务**：多模态虚假新闻检测。使用 Weibo17 和 Weibo21 数据集。
*   **Benchmark 与对比方法**：
    *   对比了 CLIP、ViLT 等基础模型，以及 HAMMER、HAMMER++、UFAFormer、ASAP、EMSF 等专门针对 DGM4 的 SOTA 方法。
    *   在虚假新闻检测中对比了 CAFE、BMR、COOLANT、MIMoE-FND 等最新模型。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA RTX 4090 GPU**。
*   **训练时长**：在 DGM4 数据集上训练约 **8 小时**。
*   **模型规模**：可训练参数量为 **319M**，低于对比方法（如 HAMMER++ 的 441M）。

#### 5. 实验数量与充分性
*   **实验规模**：在三个主流数据集上进行了大规模测试，涵盖了 12 项 DGM4 评估指标和多项虚假新闻检测指标。
*   **消融实验**：详细探讨了局部语义模态（仅标签、仅掩码、掩码-标签对）、BCV 模块的方向性、以及 HSA 模块中各组件的贡献。
*   **客观性**：通过对比不同解析器（开源词汇 vs. 标题锚定）的效果，证明了框架的通用性；同时提供了推理速度（FPS）对比，评估了模型效率。

#### 6. 主要结论与发现
*   **性能领先**：MaLSF 在 DGM4 任务的综合表现上比之前最好的方法（EMSF）提升了约 **3.3% - 4.9%**，在 Weibo 数据集上也刷新了记录。
*   **局部关联的重要性**：实验证明，单纯增加局部信息效果有限，只有将掩码与标签配对（ML Pair）并进行双向验证，才能显著提升检测精度。
*   **可解释性**：可视化结果显示，模型在做出判断时，注意力权重高度集中在被篡改的局部区域（如被替换的人脸或冲突的关键词）。

#### 7. 优点
*   **范式创新**：从“被动融合”转向“主动双向验证”，更符合人类逻辑。
*   **解决特征稀释**：通过层次化聚合和解耦设计，保留了微小的冲突信号。
*   **高效且轻量**：相比同类 SOTA 模型，参数量减少了约 27%，且保持了竞争力的推理速度（22.9 FPS）。
*   **强解释性**：掩码-标签对的权重直接反映了冲突来源，增强了模型的可信度。

#### 8. 不足与局限
*   **推理延迟**：虽然参数量更少，但由于引入了双向交叉注意力机制，其推理速度（22.9 FPS）略慢于 HAMMER++（31.1 FPS）。
*   **依赖预处理**：模型性能高度依赖于前端解析器提取掩码-标签对的质量，若解析器出错，可能影响后续验证。
*   **应用范围**：目前主要针对静态图像-文本对，尚未扩展到视频虚假信息检测领域。

（完）

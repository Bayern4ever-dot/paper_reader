---
title: Avoiding Over-smoothing in Social Media Rumor Detection with Pre-trained Propagation Tree Transformer
title_zh: 基于预训练传播树 Transformer 规避社交媒体谣言检测中的过度平滑问题
authors: "Chaoqun Cui, Caiyan Jia"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22854v1"
tags: ["query:mmfnd"]
score: 8.0
evidence: 使用传播树 Transformer 进行社交媒体谣言检测
tldr: 针对社交媒体谣言检测中图神经网络（GNN）存在的过度平滑和长程依赖捕捉能力弱的问题，本文提出了一种基于纯Transformer架构的预训练传播树模型（P2T3）。该方法通过提取对话链并引入连接信息嵌入，在避免GNN缺陷的同时，利用大规模无监督预训练提升了检测性能，为谣言检测提供了新的统一建模方案。
selection_source: fresh_fetch
motivation: 传统GNN在处理以1层节点为主的谣言传播树时易出现过度平滑，且难以捕捉长距离依赖关系。
method: 提出P2T3模型，通过提取传播树中的所有对话链，结合Token级嵌入注入结构偏置，并在大规模无标签数据上进行预训练。
result: 实验证明P2T3在多个基准数据集上超越了现有最先进方法，且在少样本场景下表现优异。
conclusion: P2T3有效解决了GNN在谣言检测中的过度平滑问题，为未来社交媒体研究提供了大模型或多模态统一方案的可能性。
---

## 摘要
深度学习谣言检测技术通常利用图神经网络（GNN）来分析帖子之间的关系。然而，这些方法在处理谣言传播结构时，常因过度平滑问题而失效，导致性能下降。我们对该问题的研究表明，过度平滑与谣言传播树的结构特征本质相关，其中大多数节点为 1 级节点。此外，GNN 难以捕捉这些树结构中的长距离依赖关系。为了规避这些挑战，我们提出了一种基于纯 Transformer 架构的预训练传播树 Transformer（P2T3）方法。该方法沿回复的传播方向从树结构中提取所有对话链，利用 token 级嵌入注入连接信息并引入必要的归纳偏置，并在大规模无标签数据集上进行预训练。实验表明，P2T3 在多个基准数据集上超越了以往的最先进方法，并在少样本条件下表现良好。P2T3 不仅避免了 GNN 固有的过度平滑问题，还可能为未来的社交媒体研究提供大模型或统一的多模态方案。

## Abstract
Deep learning techniques for rumor detection typically utilize Graph Neural Networks (GNNs) to analyze post relations. These methods, however, falter due to over-smoothing issues when processing rumor propagation structures, leading to declining performance. Our investigation into this issue reveals that over-smoothing is intrinsically tied to the structural characteristics of rumor propagation trees, in which the majority of nodes are 1-level nodes. Furthermore, GNNs struggle to capture long-range dependencies within these trees. To circumvent these challenges, we propose a Pre-Trained Propagation Tree Transformer (P2T3) method based on pure Transformer architecture. It extracts all conversation chains from a tree structure following the propagation direction of replies, utilizes token-wise embedding to infuse connection information and introduces necessary inductive bias, and pre-trains on large-scale unlabeled datasets. Experiments indicate that P2T3 surpasses previous state-of-the-art methods in multiple benchmark datasets and performs well under few-shot conditions. P2T3 not only avoids the over-smoothing issue inherent in GNNs but also potentially offers a large model or unified multi-modal scheme for future social media research.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **P2T3**（Pre-trained Propagation Tree Transformer）的方法，旨在解决社交媒体谣言检测中图神经网络（GNN）存在的过度平滑（Over-smoothing）和长程依赖捕捉能力弱的问题。以下是对该论文的深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的谣言检测方法多采用 GNN 建模谣言传播树（RPT）。然而，研究发现 GNN 在处理 RPT 时极易出现**过度平滑**现象，即随着网络层数增加，节点特征趋同，导致性能下降。
*   **研究动机**：
    *   **结构失衡**：谣言传播树通常非常扁平且不平衡，绝大多数节点（约 60%-80%）是直接回复根节点的 1 级节点。在无向图 GNN 中，这些节点互为 2 步邻居，极易导致特征混淆。
    *   **长程依赖缺失**：GNN 侧重于局部邻域聚合，难以捕捉从根节点到叶节点的完整对话链条中的深层语义和立场演变。
    *   **扩展性差**：由于过度平滑，GNN 难以通过增加深度来提升模型容量，限制了在大规模无标签数据上的预训练效果。

### 2. 核心方法论
P2T3 放弃了 GNN 架构，转而采用**纯 Transformer 架构**，通过将树结构转化为序列结构来规避 GNN 的缺陷。
*   **对话链提取**：从传播树中提取所有从 1 级节点到叶节点的路径，形成多个对话链，并按时间顺序排列。
*   **Token 级嵌入（关键技术）**：
    *   **链标识符（Chain Identifier）**：使用正交向量区分不同的对话链，确保模型能识别 Token 所属的特定对话分支。
    *   **深度嵌入（Depth Embedding）**：采用类似正弦位置编码的方式记录节点在原始树中的深度，保留层级结构信息。
    *   **类型嵌入（Type Embedding）**：标记 Token 属于源帖子、深层对话还是浅层回复。
*   **预训练策略**：
    *   在大规模无标签数据集上进行预训练。
    *   **核心公式/目标**：最大化源帖子（Root）与各对话链首个节点之间的**互信息（Mutual Information, MI）**，通过对比学习增强全局与局部表示的一致性。
*   **微调**：在有标签数据上使用交叉熵损失进行监督学习。

### 3. 实验设计
*   **数据集**：
    *   **有标签基准**：Weibo、DRWeibo、Twitter15、Twitter16。
    *   **新增无标签数据集**：UWeibo（约 21 万条）、UTwitter（约 20 万条），由作者通过爬虫采集并发布。
*   **Benchmark 与对比方法**：
    *   对比了 7 种最先进（SOTA）方法，包括基于 Transformer 的 PLAN、HD-TRANS，以及基于 GNN 的 BiGCN、GACL、RAGCL 等。
*   **实验场景**：涵盖了全量数据性能对比、消融实验、模型深度影响实验、预训练效果分析以及少样本（Few-shot）场景测试。

### 4. 资源与算力
*   **硬件**：单张 **Nvidia GeForce RTX 3090 GPU**。
*   **训练细节**：
    *   Transformer 编码器为 3 层。
    *   学习率 5e-5，Batch Size 为 32。
    *   所有模型训练 100 个 Epoch 直至收敛，取最后 10 个 Epoch 的平均值作为稳定结果。
    *   文中未明确给出具体的总训练时长，但提到单卡即可完成，说明计算开销在可控范围内。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的详尽测试。
    *   **主实验**：在 4 个数据集上进行了多次运行并给出了标准差。
    *   **消融实验**：分别验证了三种嵌入方式和预训练模块的贡献。
    *   **深度实验**：对比了 GNN 和 P2T3 在 1-8 层深度下的表现，有力证明了 P2T3 对过度平滑的免疫力。
    *   **少样本实验**：测试了样本量从 10 到 500 的表现。
*   **评价**：实验设计非常充分且逻辑严密。通过对比不同方向的信息流（TD/BU/UD）深入探讨了过度平滑的成因，实验结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **规避平滑**：P2T3 的性能随层数增加而提升并趋于稳定，而 GNN 在 3 层后性能大幅下降，证明 Transformer 更适合处理扁平的传播树。
*   **预训练红利**：P2T3 从大规模无标签数据中获益显著，性能提升幅度远超 GNN 模型。
*   **少样本优势**：在极少标注样本（如 k=50）的情况下，P2T3 凭借预训练学到的知识，表现优于其他 SOTA 模型。
*   **结构洞察**：识别出对话链中的深层节点往往包含更强烈的立场和情感，是识别谣言的关键，而 P2T3 能有效捕捉这些长程依赖。

### 7. 优点与亮点
*   **视角独特**：深入分析了谣言传播树的结构特性（1 级节点占比高）与 GNN 过度平滑之间的内在联系。
*   **架构创新**：通过 Token 级嵌入巧妙地将图结构偏置引入 Transformer，既保留了结构信息又发挥了 Transformer 处理序列的长处。
*   **资源贡献**：发布了两个大规模无标签社交媒体数据集，对半监督谣言检测研究具有重要价值。

### 8. 不足与局限
*   **序列长度限制**：对于回复极多（如数万条）的超大型传播树，提取所有对话链可能导致输入序列过长，增加计算复杂度和内存压力。
*   **数据偏差风险**：无标签数据主要来自高粉丝量账号或热门帖子，可能存在领域分布偏差，对冷门话题或初发谣言的泛化能力有待验证。
*   **多模态缺失**：目前主要聚焦于文本和结构，尚未整合社交媒体中常见的图像、视频等多模态信息。

（完）

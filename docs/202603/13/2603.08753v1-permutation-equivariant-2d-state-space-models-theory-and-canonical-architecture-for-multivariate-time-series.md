---
title: "Permutation-Equivariant 2D State Space Models: Theory and Canonical Architecture for Multivariate Time Series"
title_zh: 置换等变二维状态空间模型：多变量时间序列的理论与规范架构
authors: "Seungwoo Jeong, Heung-Il Suk"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.08753v1"
tags: ["query:tsds"]
score: 8.0
evidence: 多元时间序列动力学的规范架构
tldr: 针对多变量时间序列建模中人为设定变量顺序导致违反置换对称性的问题，本文提出了置换等变2D状态空间模型理论。通过证明置换等变线性2D系统可分解为局部自动力学和全局池化交互，作者设计了VI 2D SSM及其架构VI 2D Mamba。该方法将变量轴依赖深度从O(C)降至O(1)，在预测、分类和异常检测任务中达到了SOTA性能，验证了对称性保持建模的必要性。
selection_source: fresh_fetch
motivation: 传统多变量时间序列模型往往错误地假设变量间存在固定顺序，忽略了变量间固有的置换对称性。
method: 理论推导了置换等变线性2D状态空间系统的规范形式，并据此提出利用置换不变聚合实现变量间交互的VI 2D SSM。
result: 实验证明该模型在多个基准测试中取得领先性能，且具有更优的结构可扩展性和更低的计算复杂度。
conclusion: 保持置换等变性是多变量时间序列建模的关键，能够显著简化系统动力学并提升模型在复杂任务中的表现。
---

## 摘要
多变量时间序列（MTS）建模通常隐含地对变量施加人工排序，这违反了许多现实系统中存在的固有可交换性，即不存在规范的变量轴。我们将这一局限性形式化为对置换对称性原则的违反，并要求状态空间动力学沿变量轴具有置换等变性。在这项工作中，我们从理论上刻画了在这种对称性约束下线性变量耦合的完整规范形式。我们证明，任何置换等变的线性二维状态空间系统都会自然分解为局部自动力学和全局池化交互，这使得有序递归不仅是不必要的，而且在结构上是次优的。基于这一理论基础，我们引入了变量不变二维状态空间模型（VI 2D SSM），该模型通过置换不变聚合实现了规范等变形式。这种表述消除了沿变量轴的顺序依赖链，将依赖深度从 $\mathcal{O}(C)$ 降低到 $\mathcal{O}(1)$，并将稳定性分析简化为两个标量模式。此外，我们提出了 VI 2D Mamba，这是一种集成多尺度时间动力学和频谱表示的统一架构。在预测、分类和异常检测基准测试上的广泛实验表明，我们的模型实现了最先进的性能，并具有卓越的结构可扩展性，验证了保持对称性的二维建模的理论必要性。

## Abstract
Multivariate time series (MTS) modeling often implicitly imposes an artificial ordering over variables, violating the inherent exchangeability found in many real-world systems where no canonical variable axis exists. We formalize this limitation as a violation of the permutation symmetry principle and require state-space dynamics to be permutation-equivariant along the variable axis. In this work, we theoretically characterize the complete canonical form of linear variable coupling under this symmetry constraint. We prove that any permutation-equivariant linear 2D state-space system naturally decomposes into local self-dynamics and a global pooled interaction, rendering ordered recurrence not only unnecessary but structurally suboptimal. Motivated by this theoretical foundation, we introduce the Variable-Invariant Two-Dimensional State Space Model (VI 2D SSM), which realizes the canonical equivariant form via permutation-invariant aggregation. This formulation eliminates sequential dependency chains along the variable axis, reducing the dependency depth from $\mathcal{O}(C)$ to $\mathcal{O}(1)$ and simplifying stability analysis to two scalar modes. Furthermore, we propose VI 2D Mamba, a unified architecture integrating multi-scale temporal dynamics and spectral representations. Extensive experiments on forecasting, classification, and anomaly detection benchmarks demonstrate that our model achieves state-of-the-art performance with superior structural scalability, validating the theoretical necessity of symmetry-preserving 2D modeling.

---

## 论文详细总结（自动生成）

以下是对论文《Permutation-Equivariant 2D State Space Models: Theory and Canonical Architecture for Multivariate Time Series》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：多元时间序列（MTS）建模的核心在于捕捉时间动态和变量间的依赖关系。
*   **核心问题**：现有的二维状态空间模型（2D SSM）通常沿变量轴进行顺序扫描（Sequential Scanning）。这种做法隐含地假设变量之间存在某种人工排序或马尔可夫链结构。
*   **研究动机**：在现实世界的非空间数据中，变量通常是“可交换的”（Exchangeable），即变量的索引顺序不应影响模型结果。顺序扫描不仅引入了错误的人工偏置，还导致了 $O(C)$ 的顺序依赖链，限制了并行计算能力。本文旨在从理论上解决 2D SSM 中的置换对称性问题。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：提出**置换等变性（Permutation Equivariance）**原则。证明了在对称性约束下，任何线性变量耦合必须分解为“局部自动力学”和“全局池化交互”。
*   **关键技术细节**：
    *   **理论证明**：通过数学证明（定理1），确定了置换等变线性耦合的规范形式为 $M = \alpha I_C + \beta 11^\top$。这意味着变量间的交互应通过全局聚合而非顺序传递来实现。
    *   **VI 2D SSM**：引入全局交互场 $\psi(t)$，通过置换不变聚合函数（如均值、求和或注意力池化）提取全局特征，介导变量间的通信。
    *   **并行化**：将变量轴的依赖深度从 $O(C)$ 降低到 $O(1)$，实现了变量维度的完全并行化。
    *   **VI 2D Mamba 架构**：
        *   **多尺度时域分支**：通过不同的离散化步长 $\Delta$ 分别捕捉长期趋势和短期波动。
        *   **频谱域分支**：利用傅里叶变换在频率轴上应用 2D SSM，捕捉周期性特征。
        *   **自适应门控**：动态融合时域和频域的表示。

### 3. 实验设计
*   **数据集**：
    *   **长/短期预测**：ETT系列（7个）、Weather、Traffic、Electricity、Exchange、M4。
    *   **分类**：UEA 归档中的 10 个多变量分类数据集。
    *   **异常检测**：SMD、SWaT、PSM、MSL、SMAP。
*   **Benchmark 与对比方法**：
    *   **Transformer 类**：iTransformer, PatchTST, Crossformer, Fedformer 等。
    *   **SSM 类**：Mamba, Chimera (SOTA 2D SSM), TimePro, Simba。
    *   **其他**：ModernTCN, DLinear, TimesNet 等。

### 4. 资源与算力
*   **算力说明**：论文对比了不同模型的 **FLOPs（浮点运算数）** 和 **Peak GPU Memory（峰值显存）**。
*   **具体细节**：文中展示了在 ECL 数据集上，VI 2D Mamba 的 FLOPs 显著低于 S-Mamba 和 PatchTST。
*   **硬件型号**：提取文本中未明确提及具体的 GPU 型号（如 A100 或 V100）及总训练时长，但提供了“秒/轮”（sec/epoch）的效率对比数据。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了四大类主流时间序列任务，使用了超过 25 个基准数据集。
*   **消融实验**：针对长/短时分支、频域分支、不同的聚合函数（Mean vs Attention）以及离散化步长 $\Delta_f$ 进行了详细的消融研究。
*   **鲁棒性测试**：通过受控模拟实验（VAR过程），验证了模型在变量随机置换下的性能稳定性。
*   **充分性评价**：实验设计非常全面且客观，不仅关注准确率，还深入分析了计算效率和结构可扩展性，足以支撑其理论论点。

### 6. 主要结论与发现
*   **理论价值**：置换等变性是多元时间序列 2D 建模的理论基石，顺序扫描在结构上是次优的。
*   **性能表现**：VI 2D Mamba 在长程预测和异常检测任务中达到了 SOTA 性能，在分类任务中也极具竞争力。
*   **效率提升**：相比传统的 2D SSM（如 Chimera），该方法在处理高维变量（大 $C$ 值）时具有显著的推理速度优势和更低的内存占用。

### 7. 优点
*   **理论驱动**：并非单纯的架构微调，而是从对称性原理出发，为 2D SSM 提供了严谨的数学基础。
*   **高扩展性**：$O(1)$ 的变量依赖深度解决了 SSM 在多变量场景下的计算瓶颈。
*   **多域融合**：成功将时域的多尺度特性与频域的全局特性整合进统一的置换等变框架中。

### 8. 不足与局限
*   **分类任务局限**：在某些 UEA 分类数据集上，性能略低于 Chimera。这可能暗示在某些特定任务中，变量的特定排列顺序可能携带了模型尚未完全捕捉的隐含信息。
*   **频域参数敏感**：频域分支的稳定性高度依赖于离散化步长 $\Delta_f$ 的选择，需要针对不同数据进行调优。
*   **架构复杂性**：三分支（长、短、频）的设计虽然强大，但相比单分支模型，其超参数空间更大，实现复杂度更高。

（完）

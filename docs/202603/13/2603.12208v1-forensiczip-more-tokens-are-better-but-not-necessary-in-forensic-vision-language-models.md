---
title: "ForensicZip: More Tokens are Better but Not Necessary in Forensic Vision-Language Models"
title_zh: ForensicZip：取证视觉语言模型中 Token 越多越好但并非必要
authors: "Yingxin Lai, Zitong Yu, Jun Wang, Linlin Shen, Yong Xu, Xiaochun Cao"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12208v1"
tags: ["query:mmfnd"]
score: 10.0
evidence: 用于多媒体取证和伪造检测的多模态大语言模型
tldr: "针对多模态大模型在多媒体取证中计算开销大的问题，本文提出ForensicZip框架。不同于传统的语义驱动剪枝，该方法从伪造驱动视角出发，利用出生-死亡最优传输模型量化时间不连续性，并结合高频先验提取关键取证证据。在仅保留10%标记的情况下，该框架实现了显著的推理加速和计算量降低，同时保持了最先进的伪造检测性能。"
selection_source: fresh_fetch
motivation: 现有的视觉标记剪枝方法多为语义驱动，容易忽略背景区域中隐藏的高频异常和时间抖动等关键伪造痕迹。
method: 提出一种无需训练的框架，通过出生-死亡最优传输模型量化标记演化，并结合高频先验在大幅压缩下分离取证证据与语义内容。
result: "在Deepfake和AIGC基准测试中，该方法在10%标记保留率下实现了2.97倍加速和超过90%的FLOPs削减。"
conclusion: 实验证明，在多媒体取证任务中，通过伪造驱动的标记压缩，少量关键标记足以维持极高的检测精度与效率。
---

## 摘要
多模态大语言模型 (MLLMs) 通过为伪造检测生成文本解释，实现了可解释的多媒体取证。然而，处理密集的视觉序列会产生高昂的计算成本，特别是对于高分辨率图像和视频。视觉 Token 剪枝是一种实用的加速策略，但现有方法主要由语义驱动，往往在保留显著对象的同时丢弃了背景区域，而高频异常和时间抖动等篡改痕迹通常存在于这些背景区域中。为了解决这一问题，我们引入了 ForensicZip，这是一个无需训练的框架，它从伪造驱动的角度重新定义了 Token 压缩。ForensicZip 将 Token 的时间演化建模为带有松弛虚拟节点的生灭最优传输 (Birth-Death Optimal Transport) 问题，从而量化指示瞬态生成伪影的物理不连续性。取证评分进一步将基于传输的新颖性与高频先验相结合，以便在大比例压缩下将取证证据与语义内容分离。在 Deepfake 和 AIGC 基准测试上的实验表明，在 10% 的 Token 保留率下，ForensicZip 实现了 2.97 倍的加速和超过 90% 的 FLOPs 减少，同时保持了最先进的检测性能。

## Abstract
Multimodal Large Language Models (MLLMs) enable interpretable multimedia forensics by generating textual rationales for forgery detection. However, processing dense visual sequences incurs high computational costs, particularly for high-resolution images and videos. Visual token pruning is a practical acceleration strategy, yet existing methods are largely semantic-driven, retaining salient objects while discarding background regions where manipulation traces such as high-frequency anomalies and temporal jitters often reside. To address this issue, we introduce ForensicZip, a training-free framework that reformulates token compression from a forgery-driven perspective. ForensicZip models temporal token evolution as a Birth-Death Optimal Transport problem with a slack dummy node, quantifying physical discontinuities indicating transient generative artifacts. The forensic scoring further integrates transport-based novelty with high-frequency priors to separate forensic evidence from semantic content under large-ratio compression. Experiments on deepfake and AIGC benchmarks show that at 10\% token retention, ForensicZip achieves $2.97\times$ speedup and over 90\% FLOPs reduction while maintaining state-of-the-art detection performance.

---

## 论文详细总结（自动生成）

以下是对论文《ForensicZip: More Tokens are Better but Not Necessary in Forensic Vision-Language Models》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：多模态大语言模型（MLLM）在多媒体取证（如检测 Deepfake 或 AI 生成内容）中虽然表现出色，但处理高分辨率图像或长视频时，密集的视觉 Token 会导致巨大的计算开销（尤其是 Self-attention 的平方级复杂度），成为推理瓶颈。
*   **研究动机**：
    *   **语义与取证的错位**：现有的 Token 剪枝方法大多是“语义驱动”的（保留物体，丢弃背景）。然而，伪造痕迹（如高频噪声、边缘缝隙、时间抖动）往往存在于语义不显著的背景区域。
    *   **性能崩溃**：在极端压缩比例下，传统方法会因丢弃关键取证证据而导致检测性能剧烈下降。
    *   **物理不连续性**：生成模型在时空维度上常违反物理连续性，表现为特征的突然“出生”或“死亡”。

### 2. 方法论
ForensicZip 是一个**无需训练（Training-free）**的推理加速框架，核心思想是从“伪造驱动”的角度进行 Token 稀疏化。
*   **传输新颖性估计 (TNE)**：
    *   将相邻帧间的 Token 演化建模为**生灭最优传输 (Birth-Death Optimal Transport)** 问题。
    *   引入一个**虚拟节点（Dummy Node）**作为松弛项。当目标帧中的 Token 在前一帧找不到匹配项时，其质量会被路由至虚拟节点，从而产生“出生”成本。
    *   通过求解 Sinkhorn 迭代，量化每个 Token 的时间不连续性，捕捉瞬态生成伪影。
*   **取证评分 (FS)**：
    *   **空间调制**：利用拉普拉斯算子（Laplacian Operator）提取高频先验，捕捉重采样、混合边缘等空间异常。
    *   **融合评分**：将时间维度的传输成本与空间维度的高频响应通过乘法结合，形成“软与门”机制。只有在时空两域均表现异常的 Token 才会获得高分。
*   **物理 Top-K 选择**：根据最终评分保留前 $K$ 个 Token，直接缩减序列长度并传递给 LLM 解码器。

### 3. 实验设计
*   **数据集**：
    *   **通用/AIGC 检测**：FakeVLM (FakeClue, LOKI), DMimage, FakeShield-AIGC, SIDA。
    *   **Deepfake 取证**：DD-VQA, FakeShield-DeepFake, DFFD。
    *   **传统篡改与幻觉测试**：FakeShield-PhotoShop, POPE。
*   **基准模型 (Backbones)**：LLaVA-OneVision-7B, SIDA-7B/13B, FakeVLM, FakeShield。
*   **对比方法**：FastV, SparseVLM, VisionTrim, LLaVA-Scissor 等最先进的训练无关剪枝方法。

### 4. 资源与算力
*   **硬件环境**：实验主要在 **NVIDIA A100 (80GB)** GPU 上进行。
*   **算力消耗**：
    *   由于是 **Training-free** 方法，无需训练算力。
    *   推理阶段，最优传输（OT）求解器的计算开销相对于 Transformer 层的节省几乎可以忽略不计（仅增加几毫秒延迟，但换取了大幅的整体加速）。

### 5. 实验数量与充分性
*   **实验规模**：在超过 8 个主流基准数据集上进行了广泛测试，涵盖了图像、视频、人脸及通用场景。
*   **消融实验**：非常充分。分别验证了 TNE 模块、FS 模块、不同 OT 形式（平衡 vs 生灭）、不同空间算子（Sobel vs Laplacian）以及超参数（正则化强度、迭代次数等）对性能的影响。
*   **客观性**：通过对比不同 Token 保留率（50%, 25%, 10%）下的性能曲线，客观展示了该方法在极端压缩下的优越性。

### 6. 主要结论与发现
*   **高效加速**：在仅保留 **10% Token** 的情况下，实现了 **2.97 倍** 的端到端加速，并减少了超过 **90%** 的 FLOPs。
*   **性能保持**：在大幅压缩时，ForensicZip 依然能保持与全量 Token 相当的检测精度（如在 FakeClue 上仅下降 0.1% 左右），而传统方法在相同比例下性能会发生崩溃。
*   **精度增益**：在 POPE 任务中，该方法甚至略微提升了原始模型的精度，表明剔除冗余语义噪声有助于模型更专注于关键证据。

### 7. 优点
*   **视角创新**：敏锐捕捉到取证任务中“证据与语义正交”的特性，打破了传统的语义剪枝范式。
*   **数学工具应用得当**：引入生灭最优传输理论，为量化生成伪影提供了严谨的数学框架。
*   **实用性强**：无需重新训练，即插即用，显著降低了 MLLM 在取证领域的部署门槛。

### 8. 不足与局限
*   **超参数敏感性**：虽然实验证明在一定范围内稳定，但生灭惩罚项 $c$ 和时空权重等参数仍需根据不同模型或场景进行微调。
*   **单帧局限性**：在处理单张图像时，由于缺乏时间维度，该方法退化为基于均值偏移和高频先验的评分，无法发挥最优传输在捕捉动态异常方面的完整威力。
*   **语义伪造挑战**：对于不产生物理/高频异常、仅在高级逻辑上造假的伪造内容，该方法基于物理不连续性的假设可能不再适用。

（完）

---
title: "TimeSqueeze: Dynamic Patching for Efficient Time Series Forecasting"
title_zh: TimeSqueeze：用于高效时间序列预测的动态分块技术
authors: "Sravan Kumar Ankireddy, Nikita Seleznev, Nam H. Nguyen, Yulun Wu, Senthil Kumar, Furong Huang, C. Bayan Bruss"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11352v1"
tags: ["query:tsds"]
score: 9.0
evidence: 时序数据的内容感知分割
tldr: 针对Transformer时间序列模型在点对点嵌入（效率低）与固定长度分块（破坏局部动态）之间的权衡问题，本文提出TimeSqueeze。该方法引入动态分块机制，根据信号复杂度自适应选择分块边界。通过轻量级状态空间编码器提取特征后，对信息密集区采用短分块，平滑区采用长分块，实现变分辨率压缩。实验证明，该方法在保持时间结构的同时，显著提升了预训练效率和长程预测精度。
selection_source: fresh_fetch
motivation: 传统方法在点对点嵌入的高保真度与固定分块的高效率之间存在难以调和的矛盾。
method: 采用动态分块机制，结合状态空间编码器根据局部信号复杂度自适应调整分块边界和长度。
result: 在预训练中实现了20倍的收敛加速和8倍的数据效率提升，且预测性能优于固定分块和点对点基准。
conclusion: TimeSqueeze通过内容感知的变分辨率压缩，有效平衡了时间序列建模的计算效率与特征表达能力。
---

## 摘要
基于 Transformer 的时间序列基础模型在分词（tokenization）选择上面临着根本性的权衡：逐点嵌入（point-wise embeddings）保留了时间保真度，但随序列长度的扩展性较差；而固定长度的分块（fixed-length patching）通过施加统一边界提高了效率，但这可能会破坏自然过渡并模糊具有信息量的局部动态。为了解决这些局限性，我们引入了 TimeSqueeze，这是一种动态分块机制，能够根据局部信号复杂度自适应地选择每个序列中的分块边界。TimeSqueeze 首先应用轻量级状态空间编码器来提取全分辨率的逐点特征，然后通过将短分块分配给信息密集区域、将长分块分配给平滑或冗余段，来进行内容感知分割。这种可变分辨率压缩在保留关键时间结构的同时，大幅减少了提交给 Transformer 骨干网络的 token 序列。特别是在大规模预训练中，与等效的逐点 token 基线相比，TimeSqueeze 实现了高达 20 倍的收敛速度提升和 8 倍的数据效率提升。在长程预测基准测试中的实验表明，TimeSqueeze 的表现始终优于使用逐点分词或固定大小分块的同类架构。

## Abstract
Transformer-based time series foundation models face a fundamental trade-off in choice of tokenization: point-wise embeddings preserve temporal fidelity but scale poorly with sequence length, whereas fixed-length patching improves efficiency by imposing uniform boundaries that may disrupt natural transitions and blur informative local dynamics. In order to address these limitations, we introduce TimeSqueeze, a dynamic patching mechanism that adaptively selects patch boundaries within each sequence based on local signal complexity. TimeSqueeze first applies a lightweight state-space encoder to extract full-resolution point-wise features, then performs content-aware segmentation by allocating short patches to information-dense regions and long patches to smooth or redundant segments. This variable-resolution compression preserves critical temporal structure while substantially reducing the token sequence presented to the Transformer backbone. Specifically for large-scale pretraining, TimeSqueeze attains up to 20x faster convergence and 8x higher data efficiency compared to equivalent point-token baselines. Experiments across long-horizon forecasting benchmarks show that TimeSqueeze consistently outperforms comparable architectures that use either point-wise tokenization or fixed-size patching.

---

## 论文详细总结（自动生成）

以下是对论文《TimeSqueeze: Dynamic Patching for Efficient Time Series Forecasting》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：Transformer架构在时间序列预测中面临“保真度”与“效率”的权衡。**逐点嵌入（Point-wise）**能捕捉细微变化但计算复杂度随序列长度呈平方级增长；**固定分块（Fixed-length Patching）**虽提高了效率，但无视信号内容的异质性，容易破坏自然过渡并模糊局部动态。
*   **核心问题**：如何设计一种既能保持高频信号细节，又能在大规模长序列预训练中实现极高计算效率的通用分词器（Tokenizer）？
*   **整体含义**：论文提出了 **TimeSqueeze**，这是首个实现“序列内动态分块”的机制。它根据信号的局部复杂度自适应调整分块大小，从而在大幅减少Token数量的同时保留关键的时间结构。

### 2. 方法论
TimeSqueeze 采用了一种混合多分辨率架构，包含以下关键技术：
*   **SSM 编码器与解码器**：利用 **Mamba (State-Space Model)** 层的线性复杂度优势，在全分辨率下提取细粒度特征。编码器负责生成原始分辨率的嵌入，解码器负责将骨干网络的输出与编码器的残差特征结合。
*   **动态分块机制（核心思想）**：
    *   **内容感知分割**：不使用固定长度，而是通过计算相邻样本的绝对差值与局部平均功率（Local Power）的比值来确定边界。
    *   **判定公式**：当 $|x_i - x_{i-1}| > \tau \sqrt{P_i}$ 时（其中 $P_i$ 为滑动窗口内的平均功率，$\tau$ 为敏感度阈值），判定为新分块的起点。
    *   **变分辨率压缩**：在信息密集（波动剧烈）区域分配短分块，在平滑或冗余区域分配长分块。
*   **骨干网络与预测头**：采用混合专家模型（MoE）Transformer（如 Time-MoE）处理压缩后的 Token。预测头支持多预测视界（Multi-horizon），可同时优化不同长度的预测任务。
*   **位置信息保留**：显式保留下采样前原始位置的 ID，确保在压缩空间内仍能维持绝对时间相干性。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：使用 Time-300B 数据集（包含 3000 亿个时间点，涵盖天气、交通、金融等）。
    *   **下游评估**：在 ETT (h1, h2, m1, m2) 和 Weather 数据集上进行零样本（Zero-shot）和全样本（Full-shot）预测。
*   **Benchmark 与对比方法**：
    *   **基准模型**：Time-MoE（逐点嵌入的 SOTA 模型）。
    *   **对比模型**：Moirai, TimesFM, Moment, Chronos, iTransformer, PatchTST, DLinear 等。
*   **场景**：涵盖单变量长程预测、多变量预测以及不同骨干网络（MoE vs. 非 MoE）的泛化性测试。

### 4. 资源与算力
*   **硬件环境**：使用 **2 张 NVIDIA A100 80GB GPU**。
*   **训练配置**：预训练 100,000 步，Batch Size 为 256，最大上下文长度 2048。
*   **效率提升**：
    *   相比逐点嵌入基线，训练内存占用减少 **3.4 倍**。
    *   训练时间缩短约 **20 倍**。
    *   推理吞吐量在长预测视界下提升高达 **10.5 倍**。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的详尽实验，包括 5 个主流数据集的零样本/全样本对比、6 个数据集的多变量预测对比。
*   **消融实验**：非常充分。对比了“动态 vs 固定分块”、“SSM vs 线性编码器”、残差连接的必要性、位置编码的影响、预训练上下文长度的缩放效应等。
*   **客观性**：通过在不同骨干网络（如 Encoder-only）和不同预训练数据集（如 GiftEvalPretrain）上复现增益，证明了该方法的通用性和公平性。

### 6. 主要结论与发现
*   **高效性**：TimeSqueeze 仅需 1/8 的数据量和 1/20 的训练时间，即可达到与逐点嵌入模型相当的预测精度。
*   **长上下文优势**：增加预训练时的上下文长度能显著提升推理性能，即使推理时使用的上下文较短。
*   **动态分块的优越性**：动态分块在所有测试中均优于固定大小分块，证明了“按需分配计算资源”的有效性。
*   **SSM 的适配性**：SSM 结构作为时间序列的归纳偏置（Inductive Bias），在处理全分辨率特征提取方面优于传统的线性投影。

### 7. 优点
*   **真正的动态性**：不同于以往针对整个序列调整分块大小的方法，TimeSqueeze 实现了序列内部的自适应调整。
*   **极高的算力性价比**：为资源受限的环境下训练大规模时间序列基础模型提供了可行路径。
*   **即插即用**：作为一种 Tokenizer 机制，它可以无缝集成到现有的各种 Transformer 骨干网络中。
*   **数学简单且鲁棒**：基于信号功率的阈值判定方法不依赖复杂的外部模型，且对信号幅值具有缩放不变性。

### 8. 不足与局限
*   **超参数敏感性**：虽然 $\tau$ 具有一定的鲁棒性，但为了达到特定的平均压缩率，仍需针对不同性质的数据集进行微调。
*   **高压缩率下的性能损失**：当目标压缩率超过 6x 或 8x 时，预测精度会出现较明显的下降，可能需要更复杂的层次化压缩技术来弥补。
*   **架构复杂度**：相比简单的 Patching，引入 SSM 编码器/解码器增加了模型实现的复杂度和推理时的初始延迟（尽管被 Token 减少带来的增益抵消）。

（完）

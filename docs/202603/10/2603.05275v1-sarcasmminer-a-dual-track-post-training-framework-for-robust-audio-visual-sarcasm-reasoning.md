---
title: "SarcasmMiner: A Dual-Track Post-Training Framework for Robust Audio-Visual Sarcasm Reasoning"
title_zh: SarcasmMiner：一种用于鲁棒视听讽刺推理的双轨后训练框架
authors: "Zhu Li, Yongjian Chen, Huiyuan Lai, Xiyuan Gao, Shekhar Nayak, Matt Coler"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05275v1"
tags: ["query:mmfnd"]
score: 8.0
evidence: 多模态讽刺检测与跨模态推理
tldr: 针对多模态讽刺检测中跨模态不一致性导致的推理难题，本文提出 SarcasmMiner 框架。该框架通过强化学习后训练，将讽刺检测重构为结构化推理任务。采用双轨蒸馏策略初始化模型并训练生成式奖励模型（GenRM），利用 GRPO 算法结合准确率与推理质量的双重奖励进行优化，有效减少了多模态推理中的幻觉，显著提升了检测性能。
selection_source: fresh_fetch
motivation: 旨在解决多模态讽刺检测中跨文本、音频和视觉线索的语用不一致性，以及基础模型在推理过程中容易产生幻觉的问题。
method: 提出一种基于强化学习的后训练框架，通过双轨蒸馏初始化模型并训练生成式奖励模型，采用 GRPO 算法对准确率和推理质量进行解耦优化。
result: "在 MUStARD++ 数据集上，该方法将 F1 分数从零样本的 59.83% 和监督微调的 68.23% 提升至 70.22%。"
conclusion: 研究表明，感知推理的奖励建模能够显著增强多模态讽刺检测的性能和跨模态关联能力。
---

## 摘要
多模态讽刺检测需要通过跨模态推理来解决文本、声学和视觉线索之间的语用不一致性。为了利用基础模型实现鲁棒的讽刺推理，我们提出了 SarcasmMiner，这是一个基于强化学习的后训练框架，能够抵御多模态推理中的幻觉。我们将讽刺检测重新表述为结构化推理，并采用双轨蒸馏策略：高质量的教师轨迹用于初始化学生模型，而全量轨迹则用于训练生成式奖励模型 (GenRM) 以评估推理质量。学生模型通过群体相对策略优化 (GRPO) 进行优化，并对准确性和推理质量使用解耦奖励。在 MUStARD++ 数据集上，SarcasmMiner 将 F1 分数从 59.83%（零样本）和 68.23%（有监督微调）提升至 70.22%。这些研究结果表明，推理感知的奖励建模增强了性能和多模态接地。

## Abstract
Multimodal sarcasm detection requires resolving pragmatic incongruity across textual, acoustic, and visual cues through cross-modal reasoning. To enable robust sarcasm reasoning with foundation models, we propose SarcasmMiner, a reinforcement learning based post-training framework that resists hallucination in multimodal reasoning. We reformulate sarcasm detection as structured reasoning and adopt a dual-track distillation strategy: high-quality teacher trajectories initialize the student model, while the full set of trajectories trains a generative reward model (GenRM) to evaluate reasoning quality. The student is optimized with group relative policy optimization (GRPO) using decoupled rewards for accuracy and reasoning quality. On MUStARD++, SarcasmMiner increases F1 from 59.83% (zero-shot), 68.23% (supervised finetuning) to 70.22%. These findings suggest that reasoning-aware reward modeling enhances both performance and multimodal grounding.

---

## 论文详细总结（自动生成）

这是一份关于论文《SarcasmMiner: A Dual-Track Post-Training Framework for Robust Audio-Visual Sarcasm Reasoning》的结构化深度总结：

### 1. 核心问题与研究背景
*   **核心问题**：多模态讽刺检测（Multimodal Sarcasm Detection）极具挑战性，因为它依赖于文本、音频（语调）和视觉（表情）之间的“语用不一致性”（如用平淡的语调说赞美的话）。
*   **研究动机**：现有的多模态大模型（MLLMs）虽然具备一定的感知能力，但在处理复杂的讽刺推理时，往往会产生“幻觉”，即编造不存在的声学或视觉证据来强行解释其预测结果。此外，缺乏大规模的高质量讽刺推理链（CoT）数据也限制了模型的进一步优化。

### 2. 方法论
SarcasmMiner 提出了一个三阶段的后训练（Post-training）框架：
*   **阶段 1：推理流形生成**：利用强大的教师模型（Qwen3-Omni-30B）对多模态输入进行随机采样，生成包含 `<think>`（推理链）和 `<answer>`（标签）的多种推理轨迹。
*   **阶段 2：双轨蒸馏（Dual-Track Distillation）**：
    *   **轨道 A（SFT 初始化）**：筛选出预测正确且逻辑连贯的高质量轨迹，用于对学生模型（Qwen2.5-Omni-7B）进行有监督微调。
    *   **轨道 B（生成式奖励模型 GenRM）**：利用包含错误预测和幻觉推理的全量轨迹，训练一个轻量级的生成式奖励模型（Qwen2.5-3B）。该模型通过预测二进制 Token（“1”或“0”）来评估推理过程的逻辑有效性。
*   **阶段 3：基于 GRPO 的强化学习**：采用群体相对策略优化（GRPO）算法，通过**解耦奖励机制**对学生模型进行对齐。奖励函数由三部分组成：
    1.  **准确率奖励 ($R_{acc}$)**：判断最终标签是否正确。
    2.  **格式奖励 ($R_{fmt}$)**：确保输出符合结构化格式。
    3.  **推理质量奖励 ($R_{GenRM}$)**：由 GenRM 评估推理链是否真实接地（Grounding），严厉惩罚编造证据的行为。

### 3. 实验设计
*   **数据集**：使用 **MUStARD++** 数据集，包含 1,202 条英语口语语料，涵盖文本、音频和视频模态，类别分布平衡。
*   **评估指标**：准确率（Acc）、Macro-F1 分数，以及作者提出的 **GenRM 接受率（GAR）**——用于自动衡量推理逻辑的健全性。
*   **对比方法（Baselines）**：
    *   **零样本模型**：MiniCPM-o-2.6, Phi-4-Multimodal, Qwen2.5-Omni-7B (Base), Qwen3-Omni-30B (Teacher)。
    *   **消融变体**：不同 SFT 初始化策略（贪婪、Best-of-N、多样化采样）以及不同强化学习配置（有无 SFT、有无 GenRM）。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **4 张 NVIDIA H100 90GB GPU** 的节点上完成。
*   **软件框架**：使用 `ms-swift` 框架和 `DeepSpeed ZeRO-3` 优化技术。
*   **训练细节**：SFT 阶段训练 4 个 Epoch，RL（GRPO）阶段训练 2 个 Epoch。推理采样使用 `vLLM` 引擎以提高效率。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的对比，包括 5 种主流全模态大模型的零样本对比，以及针对自身框架 3 个阶段的详细消融实验（共约 8 组主要配置对比）。
*   **充分性与客观性**：
    *   实验涵盖了从模型规模（7B vs 30B）到训练策略（SFT vs RL）的全面分析。
    *   引入了混淆矩阵分析和具体案例研究（Case Study），直观展示了模型如何通过减少幻觉来提升性能。
    *   **局限性**：实验仅在 MUStARD++ 一个数据集上进行，样本量（约 1.2k）相对较小，可能存在过拟合特定数据集风格的风险。

### 6. 主要结论与发现
*   **性能超越教师模型**：经过后训练的 7B 学生模型（SarcasmMiner）在 F1 分数上达到 **70.22%**，不仅远超其基座模型（59.83%），甚至超过了 30B 的教师模型（65.89%）。
*   **有效抑制幻觉**：通过 GenRM 奖励，模型的 GAR 指标显著提升（从 SFT 的 86.04 提升至 90.43），混淆矩阵显示假阳性（FP）大幅减少，证明模型学会了仅在有确凿多模态证据时才判定为讽刺。
*   **解耦奖励的必要性**：单纯优化准确率会导致模型寻找“统计捷径”而产生幻觉，只有引入推理质量奖励才能实现真正的跨模态对齐。

### 7. 优点
*   **创新性奖励建模**：将奖励模型设计为生成式（GenRM）而非传统的标量回归，提高了训练的稳定性。
*   **高效的 RL 算法**：采用 GRPO 算法，省去了 Critic 网络，极大地节省了处理多模态数据时的显存开销。
*   **双轨策略**：充分利用了教师模型生成的“失败案例”来训练奖励模型，变废为宝。

### 8. 不足与局限
*   **数据规模限制**：MUStARD++ 数据集规模较小，且仅限于英语环境，模型在其他语言或更广泛场景下的泛化能力有待验证。
*   **依赖强教师模型**：框架的第一步高度依赖于顶级模型（如 Qwen3-30B）生成高质量推理链的能力。
*   **计算成本**：尽管使用了 GRPO，但多模态 RL 训练依然需要高性能硬件（如 H100），对普通研究者门槛较高。

（完）
